# Superpowers Plan Status

Last audited: 2026-06-28

This file is the routing source of truth for `docs/superpowers/plans/*`.
The individual plan files are historical execution plans. Some older files still
contain unchecked task boxes even though their goals have since been implemented
and merged. Do not restart a historical plan only because its original checkboxes
were not backfilled.

Use this order when entering the repo:

1. Read `AGENTS.md` for current agent operating rules.
2. Read `docs/PROJECT_BRIEF.md` for the product summary.
3. Read this file before treating any individual plan as active work.
4. Open the specific plan only for implementation details or historical context.

## Current Active Plan

| Plan | Status | Evidence |
| --- | --- | --- |
| `2026-07-01-figma-image-asset-import.md` | Blocked | PR #199 remains open. The three P2 review findings are addressed through code/test commit `c09d0c0` with regression tests, and review threads have replies. The plan stays active because GitHub reports `mergeable_state: blocked`; `restore-drill` and `retention` pass but `Vercel` fails with `Deployment rate limited - retry in 24 hours`; full PR-head local/e2e verification is not trusted while `git status --short --branch` exits `134`; and merge plus post-merge cleanup are not complete. |

## Completed Plans

| Plan | Status | Evidence |
| --- | --- | --- |
| `2026-06-28-figma-json-basic-import.md` | Completed | Added the first write-enabled external migration mapper for Figma REST file JSON. `reviewExternalMigrationArchive` now marks Figma JSON with CANVAS pages as importable, while Penpot ZIP and opaque Figma binaries remain blocked; `importExternalMigrationArchive` maps Figma `CANVAS`, `FRAME`, `RECTANGLE`, and `TEXT` nodes into a Layo `DesignFile` with basic geometry, solid fills, strokes, text content, and skipped-node warnings. `FileStorage.importExternalMigrationArchive`, `POST /migrations/external/import`, web API helpers, and the visible file-panel import button create a fresh private project/document from the reviewed Figma JSON. Remaining gaps are image asset export/package mapping, components, variants, effects, advanced geometry/style fidelity, Penpot ZIP shape mapping, and full hosted migration workflows. Verified with RED/GREEN server mapper, HTTP, web API, and Playwright CLI import-flow coverage; `pnpm run check:penpot-maturity`; `pnpm run check:design-rules`; `pnpm typecheck`; `pnpm --filter @layo/web build`; `cargo test -p editor-core`; `pnpm test`; `pnpm test:e2e` with 145 passing tests; and `git diff --check`. |
| `2026-06-28-penpot-migration-preflight.md` | Completed | Added a no-write external migration preflight for Penpot and Figma intake. `reviewExternalMigrationArchive` classifies Penpot ZIP/.penpot, Figma REST JSON, and opaque Figma binary files, including standard stored and deflated ZIP entries, and reports source, archive kind, entries, document candidates, asset candidates, blockers, warnings, and next steps while keeping `canImport: false`. `POST /migrations/external/review`, `pnpm run migration:review`, web API helpers, and a visible file-panel card expose the same review without writing projects, files, or assets. Remaining gap is actual Penpot/Figma geometry/style/asset mapping. Verified with RED/GREEN server/web/API/script tests, CLI smoke, focused and full Playwright CLI e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-28-penpot-backup-retention.md` | Completed | Added a local backup archive repository and retention automation path for persistent filesystem hosts. `writeStorageBackupToRepository`, `listStorageBackupRepository`, and `pruneStorageBackupRepository` now store reviewed `.layo` backup zip archives, list them newest-first with manifest metadata, dry-run or apply `keepLast` plus `maxAgeDays` pruning, and protect the newest recovery points before age deletion. `pnpm run storage:backup` now exposes `repository-put`, `repository-list`, and `repository-prune`; `.github/workflows/storage-backup-retention.yml` seeds three dated archives, lists them, dry-runs retention, applies retention, and verifies exactly two zip archives remain without hosted secrets. Remaining operations gaps are durable hosted database/object-store backup storage, CI-owned Vercel secrets/deploy proof, hosted multi-instance registry sync, hosted durable pub/sub, and Penpot/Figma migration. Verified with RED/GREEN storage and artifact tests, CLI repository smoke, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with Playwright CLI, and `git diff --check`. |
| `2026-06-28-penpot-storage-restore-drill.md` | Completed | Added a repository-owned scheduled restore drill for the `.layo` backup runbook. `runStorageRestoreDrill` now creates a backup, restores it into scratch storage, and verifies expected project/file/version evidence through `FileStorage`; `pnpm run storage:backup -- drill` exposes the flow for operators and CI; `.github/workflows/storage-restore-drill.yml` seeds a temporary store and runs the drill on a weekly schedule, manual dispatch, and relevant PRs without Vercel or repository-admin secrets. Remaining gaps are hosted database/object-store storage, hosted backup retention automation, hosted multi-instance registry sync, hosted durable pub/sub, and Penpot/Figma migration. Verified with RED/GREEN storage and artifact tests, CLI drill smoke, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 144 passing Playwright CLI tests, and `git diff --check`. |
| `2026-06-28-verified-github-homepage.md` | Completed | Added a guarded GitHub About homepage sync path after the stale Vercel host failure loop. `scripts/sync-github-about-homepage.mjs` validates repository/token/URL inputs, runs the live Layo deployment smoke check, and only then PATCHes GitHub About to the normalized verified URL; failed smoke checks never reach the GitHub API. The Vercel Production workflow now calls this script instead of inline PATCH code, and deployment docs make the manual reconciliation path explicit. Manual production is now verified at `https://layo-three.vercel.app/` and GitHub About points there; CI-owned production deploy proof still requires repository Vercel secrets and a workflow-returned URL that passes live smoke. Verified with RED/GREEN homepage sync tests, deployment artifact tests, dev-script tests, live stale-host failure proof, and repository gates recorded in the implementation plan. |
| `2026-06-28-penpot-storage-backup-restore.md` | Completed | Added a repository-owned `.layo` storage backup/review/restore path for persistent filesystem hosts. Layo now archives storage files under `storage/` with a readable `manifest.json`, reviews archive entries/directories/byte counts before writes, restores into a fresh storage root, refuses non-empty restore targets unless forced, and exposes the flow through `pnpm run storage:backup`. The deployment runbook documents backup, review, fresh restore, server smoke, and the remaining hosted database/object-store backup automation gap. Verified with RED/GREEN storage and artifact tests, CLI backup/review/restore smoke through `FileStorage`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck` after fixing the fixture/type-narrowing failure loop, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 144 passing Playwright CLI tests, and `git diff --check`. |
| `2026-06-28-penpot-comment-event-fanout.md` | Completed | Added shared-filesystem durable comment event fanout for open editor sessions. Comment sidecars now retain bounded live events with sequence ids for create/reply/read/resolve mutations, `GET /comments/events` replays events from shared storage over SSE with `after` cursors, the web API exposes durable event metadata and replay cursors, and the app tracks per-file comment event sequences before refreshing comments, notifications, and activity. EventSource refreshes preserve local create/reply/resolve success status text while still updating threads, notifications, and activity. Remaining gaps are hosted durable pub/sub beyond shared-filesystem event logs, full CRDT-backed live comment editing, external notification channels, full visual comment review, and branch/review/merge workflows. Verified with RED/GREEN storage, HTTP, web API, and Playwright CLI coverage; `pnpm run check:penpot-maturity`; `pnpm run check:design-rules`; `pnpm typecheck`; `pnpm --filter @layo/web build`; `cargo test -p editor-core`; `pnpm test`; `pnpm test:e2e` with 144 passing tests; and `git diff --check`. |
| `2026-06-28-penpot-registry-event-fanout.md` | Completed | Added shared-filesystem library registry event fanout for open file-panel sessions. Publishing or republishing a registry library now appends durable `library-registry` events with sequence ids and team scope, `GET /libraries/events` replays visible events over SSE from the shared `.layo` event log, the web API exposes `subscribeToLibraryRegistryEvents`, and the file panel refreshes registry update notifications from the event stream while preserving polling as fallback. Remaining gaps are hosted multi-instance registry sync with durable auth, hosted durable pub/sub beyond shared-filesystem storage, backup/restore runbooks, and external migration paths. Verified with RED/GREEN storage, HTTP, web API, and Playwright CLI coverage; `pnpm run check:penpot-maturity`; `pnpm run check:design-rules`; `pnpm typecheck`; `pnpm --filter @layo/web build`; `cargo test -p editor-core`; `pnpm test`; `pnpm test:e2e` with 144 passing tests; and `git diff --check`. |
| `2026-06-28-penpot-token-bundle-update-notifications.md` | Completed | Added Penpot-inspired token-bundle subscriptions and update notifications for shared registry libraries. Token-bundle imports now persist a per-file subscription with token, token-set, and token-theme counts plus the imported registry timestamp; republishing the same library id creates token update notifications; applying the update deliberately replaces the target file's tokens, token sets, and token themes while leaving components untouched. Storage, HTTP, MCP, web API helpers, and the file-panel UI expose list/apply token update flows, with browser polling refresh for open target files. Remaining gaps are hosted permissioned registry sync, hosted durable pub/sub beyond shared-filesystem storage, backup/restore runbooks, external migration paths, and arbitrary filter/effect graph fidelity. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI coverage. |
| `2026-06-28-penpot-library-token-bundle-import.md` | Completed | Added Penpot-inspired shared-library token bundle review/import. Published `.layo-library.zip` archives now include token sets and token themes, registry token review reports the replacement scope before writing, and import replaces the target file's tokens, token sets, and token themes without touching components. Storage, HTTP, MCP, web API helpers, and the file-panel UI expose the flow, with Playwright CLI coverage for reviewing and importing a Team Kit token bundle from the shared registry. Remaining gaps are live theme subscription/update notifications, hosted permissioned registry sync, hosted durable pub/sub beyond shared-filesystem storage, backup/restore runbooks, and external migration paths. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 143 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-library-update-notifications.md` | Completed | Added Penpot-inspired shared-library registry subscriptions and update notifications. Imports now persist a per-file subscription with the imported registry timestamp and component/token id maps; republishing the same library id creates update notifications; applying an update reuses existing id maps, adds only new library members, and avoids duplicate imported components. Storage, HTTP, MCP, web API helpers, and the file-panel UI expose list/apply update flows, with quiet browser polling for open target files. Remaining gaps are live theme subscription/update notifications, hosted permissioned registry sync, hosted durable pub/sub beyond shared-filesystem storage, backup/restore runbooks, and external migration paths. Verified with RED/GREEN focused storage, HTTP, MCP, web API, web typecheck, focused Playwright CLI, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 142 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-library-registry-mcp.md` | Completed | Closed the MCP side of the Penpot-inspired shared-library registry. Layo now exposes `publish_library_registry_item`, `list_library_registry`, `review_library_registry_item`, and `import_library_registry_item` with read/write safety annotations and storage-backed publish/list/no-write-review/import behavior. Remaining gaps are live library subscription/update notifications, permissioned hosted registry sync, cross-process deployment storage, backup/restore runbooks, and external Penpot/Figma migration paths. Verified with RED failure for missing MCP tools, focused GREEN MCP coverage, `pnpm --filter @layo/server exec vitest run src/mcp.test.ts`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 142 passing tests, `git diff --check`, and squash merge PR #183 as `11160c2165da3703c6afc4fdc1337befac9f4dde` with post-merge branch/worktree cleanup recorded. |
| `2026-06-28-penpot-library-registry-sync.md` | Completed | Added a Penpot-inspired server-local published library registry. Layo now stores the latest `.layo-library.zip` per registry id, lists published libraries, reviews them before import, and imports reusable components/tokens across projects through storage, HTTP, web API helpers, and visible file-panel controls. The MCP registry-tool follow-up was closed by `2026-06-28-penpot-library-registry-mcp.md`; remaining gaps are live library subscription/update notifications, permissioned hosted registry sync, cross-process deployment storage, and external migration paths. Verified with RED/GREEN focused storage, HTTP, web API, and Playwright CLI coverage; `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 142 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-raster-effect-fidelity.md` | Completed | Added Penpot-inspired selected-layer PNG/JPEG/WEBP raster shadow-stack fidelity across Konva layer rendering, shadow-aware crop bounds, and Dev Panel decoded-image Playwright inspection. Remaining design-system gaps are arbitrary filter/effect graph fidelity and hosted theme/library registry sync. Verified with RED/GREEN focused Playwright CLI raster proof, focused effect-shadow state coverage, PNG scale regression coverage, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 141 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-effect-artifact-fidelity.md` | Completed | Added Penpot-inspired selected-layer SVG/PDF effect-shadow artifact fidelity across expanded SVG viewBox/filter output, single-root SVG defs for child shadows, PDF media box/resources, and Dev Panel download inspection. Remaining design-system gaps are richer raster effect fidelity and hosted theme/library registry sync. Verified with RED/GREEN node-artifact unit tests, a nested child-shadow SVG defs regression test, focused Playwright CLI download proof, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 140 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-multi-effect-stack.md` | Completed | Added Penpot-inspired ordered multi-effect shadow stacks across saved style metadata, right Inspector stack editing, agent/MCP persistence, Rust round-trip, and CSS/structure handoff. Remaining design-system gaps are richer raster/vector effect fidelity and hosted theme/library registry sync. Verified with RED/GREEN focused server/web/Rust tests, generated binding export, focused Playwright CLI proof, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 139 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-effect-shadow-overrides.md` | Completed | Added the first Penpot-inspired effect/style override fidelity slice. Nodes now persist optional `style.effect_shadow` metadata across renderer/server/Rust/MCP contracts, the right Inspector edits and persists the shadow through `set_node_style`, the canvas renders conservative CSS-like shadows, component-instance source-tree variant switching preserves compatible effect-shadow overrides, and code export includes CSS `box-shadow`, structure `effectShadow`, and style handoff annotation text. Remaining design-system gaps are richer multi-effect/style-token fidelity and hosted theme/library registry sync. Verified with RED/GREEN focused web reducer, server storage, server code export, Rust round-trip, and Playwright CLI Inspector coverage; full repository gates are recorded in the implementation plan. |
| `2026-06-28-penpot-variant-source-viewport-reorder.md` | Completed | Added Penpot-inspired direct viewport source reorder handles for selected main-component variant areas. Dragging a source handle reorders the saved `ComponentDefinition.variants` array through the existing component variant reducer and HTTP persistence path, then reflows `component.source_node`, variant source nodes, and live canvas source nodes so instances consume the new order. Remaining design-system gaps are broader effect/style override fidelity and hosted theme/library registry sync. Verified with RED/GREEN web reducer and server storage tests, RED/GREEN Playwright CLI source-handle coverage, web/server typechecks, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 135 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-area-layout-metadata.md` | Completed | Added Penpot-inspired component variant-area layout metadata. Renderer/server/Rust contracts now persist `ComponentDefinition.variant_area`; generated bindings include `ComponentVariantArea`; web reducer supports undo/redo-safe `set_component_variant_area`; server HTTP, MCP, and agent command batches can edit the layout; and the right Inspector exposes Korean-first layout, gap, and padding controls for selected main components. Remaining design-system gaps are direct canvas variant-area visual editing/reflow, broader effect/style override fidelity, and hosted theme/library registry sync. Verified with RED/GREEN web reducer, server HTTP/agent, Rust round-trip, generated binding, and Playwright CLI Inspector persistence coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 134 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-area-visual-reflow.md` | Completed | Added Penpot-inspired variant-area visual reflow. Combining sibling main components now reflows generated variant sources into the default horizontal area, Inspector/agent/storage variant-area edits reflow source nodes and matching live canvas nodes using stable padding-aware origins, web undo restores pre-combine source positions, and selected main components show a canvas variant-area outline. Remaining design-system gaps are direct viewport handles for editing variant areas, broader effect/style override fidelity, and hosted theme/library registry sync. Verified with RED/GREEN web reducer, server storage/agent, and focused Playwright CLI coverage, plus web/server typechecks. |
| `2026-06-28-penpot-variant-area-viewport-handles.md` | Completed | Added Penpot-inspired direct viewport gap handles for selected main-component variant areas. The canvas outline now exposes horizontal and vertical draggable gap handles that update saved `ComponentDefinition.variant_area.gap` through the existing reducer and persistence path, reusing the landed reflow behavior so source nodes and live canvas nodes move together. Remaining design-system gaps are direct viewport reordering of variant sources, broader effect/style override fidelity, and hosted theme/library registry sync. Verified with RED/GREEN Playwright CLI coverage, focused variant reflow reducer tests, and web typecheck. |
| `2026-06-28-penpot-variant-style-geometry-overrides.md` | Completed | Added Penpot-inspired component-instance stroke, stroke width, opacity, and geometry override preservation while switching variants. Web editor state, server storage geometry updates, and agent command batches now record compatible descendant overrides by source node id; agent command batches also support `set_node_style` for saved style mutations. Root-instance placement stays outside overrides because variant switching already preserves instance transform separately. Remaining design-system gaps are full variant-area layout editing, broader effect/style override fidelity, and hosted theme/library registry sync. Verified with RED/GREEN focused web reducer, server storage, and agent command tests, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 133 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-fill-override-preservation.md` | Completed | Added Penpot-inspired component-instance fill override preservation while switching variants. Web editor state and server storage now record manual fill changes on component-instance roots or descendants into `component_instance.overrides` using source node ids, and variant source-tree materialization reapplies compatible fill overrides alongside text overrides. Remaining design-system gaps are full variant-area layout editing, stroke/opacity/geometry override preservation, and hosted library registry sync. Verified with RED/GREEN focused web reducer and server storage tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 133 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-source-switching.md` | Completed | Added Penpot-inspired variant-owned source trees and component-instance source-tree materialization. Renderer/server/Rust contracts now persist optional `ComponentVariant.source_node`; web and storage create instances from the selected variant source, switch instances by materializing the target source tree, preserve compatible text overrides by source node id, and keep generated combine-as-variants source trees. Agent combine/create paths use the same source-tree semantics. Full Penpot variant-area layout editing, non-text override preservation, and hosted library registry sync remain design-system gaps. Verified with RED/GREEN web reducer, server storage, and Rust round-trip tests, focused Playwright CLI source-switch proof, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 133 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-override-preservation.md` | Completed | Added Penpot-inspired component-instance text override preservation while switching variants. Web editor state now records root and nested component-instance text edits into `component_instance.overrides` using source node ids, removes the override when undo restores the source text, and preserves existing selected-instance variant semantics. Source-tree switching was later closed by `2026-06-28-penpot-variant-source-switching.md`; full variant-area layout editing, non-text override preservation, and hosted theme/library registry sync remain design-system gaps. Verified with RED/GREEN reducer coverage, focused component/server regressions, Playwright CLI variant authoring and matrix flows, direct Playwright API-log proof for selected-instance variant controls, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `pnpm test:e2e` with 133 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-token-theme-matrix.md` | Completed | Added a Penpot-inspired grouped token theme matrix to the right Inspector. The matrix shows token themes grouped by theme group against document token sets, exposes membership checkboxes, shows included-set priority badges from each theme's ordered `token_set_ids`, and reuses the existing `upsert_token_theme` path so active bound fills rematerialize without adding new document or MCP commands. Remaining design-system gaps are full variant-area layout editing, non-text override preservation, and hosted theme/library registry sync. Verified with RED/GREEN Playwright CLI coverage, direct Playwright API-log interaction proof, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `pnpm test:e2e` with 133 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-token-theme-reorder.md` | Completed | Added Penpot-inspired token theme ordering and theme token-set priority ordering. Server agent commands and MCP schemas now support `reorder_token_theme` and `reorder_token_theme_set`, active token resolution respects enabled theme `token_set_ids` order as the override order, the web reducer supports undo/redo-safe order snapshots with binding rematerialization, and the right Inspector exposes Korean-first up/down controls for theme rows and included token sets. Remaining design-system gaps are full variant-area layout editing, non-text override preservation, and hosted theme/library registry sync. Verified with RED/GREEN server, web reducer, and Playwright CLI e2e coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `pnpm test:e2e` with 132 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-token-theme-management.md` | Completed | Added Penpot-inspired document-local token theme management. Server agent commands and MCP schemas now support `upsert_token_theme` and `delete_token_theme`, the web reducer supports undo/redo-safe theme snapshots with binding rematerialization, and the right Inspector can create, rename, regroup, edit token-set membership, enable, and delete token themes without rewriting raw DTCG JSON. Theme reorder was later closed by `2026-06-28-penpot-token-theme-reorder.md`; remaining design-system gaps are visual theme matrix management and hosted theme/library registry sync. Verified with RED/GREEN server, web reducer, and Playwright CLI coverage, imported-theme regression coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `pnpm test:e2e` with 131 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-combine-components-as-variants.md` | Completed | Added Penpot-inspired context-menu combine-as-variants for selected sibling main component sources. Web state now combines default-only component definitions into one generated variant component with undo/redo snapshots, server agent commands and MCP schemas expose `combine_components_as_variants`, and the right-click `컴포넌트` menu exposes `변형으로 결합` only for valid multi-selected sibling component sources. Generated variant source preservation was later closed by `2026-06-28-penpot-variant-source-switching.md`; full variant-area layout editing, non-text override preservation, and hosted library registry/update sync remain design-system gaps. Verified with RED/GREEN reducer, server, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 129 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-matrix-editing.md` | Completed | Added Penpot-inspired direct component variant matrix editing in the right Inspector. Matrix cells now edit saved variant property values through the existing `set_component_variants` path, row editors stay synchronized, property columns can be deleted with a last-property guard, variants can be deleted with the existing invalid-instance fallback semantics, and duplicate-combination warnings continue to work against edited values. Remaining design-system gaps are richer theme/mode management, bulk matrix generation, and hosted library registry/update sync. Verified with RED/GREEN Playwright CLI coverage, focused reducer fallback coverage, headed Playwright CLI proof, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 128 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-variant-property-types.md` | Completed | Added Penpot-inspired explicit component variant property type authoring. Renderer/server/Rust contracts now persist `select` and `boolean` property types, legacy missing types default to `select`, code export preserves type metadata, the right Inspector selected-main-component variant editor exposes Korean-first type controls, and selected instances render explicit boolean properties as toggles while explicit select properties stay selects even with `true`/`false` values. Remaining design-system gaps are richer theme/mode management, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN Rust, HTTP, web state, code export, Playwright CLI coverage, direct Playwright interaction proof, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 127 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-style-library-organization.md` | Completed | Added Penpot-inspired reusable-style organization for document-local color and typography styles. Server agent commands and MCP schemas now support `duplicate_style`; web editor state supports undo/redo-safe style duplication; the right Inspector style manager supports search, color/typography filtering, name and usage sorting, slash-name grouping, and duplicate actions persisted through the agent command route. Remaining design-system gaps are richer theme/mode management, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused server and web state tests, Playwright CLI style organization coverage, and full-suite gates recorded in the implementation plan. |
| `2026-06-28-penpot-style-management.md` | Completed | Added Penpot-inspired reusable style management for document-local color and typography styles. Server agent commands and MCP schemas now support `rename_style` and `delete_style`, `inspectCanvas` exposes style `usageCount` and `usedBy`, deleted styles clear fill/typography bindings without reverting materialized visual values, web editor state supports undo/redo-safe rename/delete snapshots, and the right Inspector lists styles with usage counts plus rename/delete controls persisted through the agent command route. Remaining design-system gaps are style duplication/grouping/search/sort/filter, richer theme/mode management, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused server and web state tests, Playwright CLI reusable-style coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 125 passing tests, and `git diff --check`. |
| `2026-06-27-penpot-reusable-styles-baseline.md` | Completed | Added Penpot-inspired document-local reusable color and typography styles across renderer/server/Rust document contracts, generated TypeScript bindings, deterministic agent commands and MCP schemas, validation, undo/redo-safe web state, right Inspector create/apply controls, persisted selected-node/text bindings, and code export metadata. Also hardened `pnpm test:e2e` with a local service runner so Playwright CLI no longer depends on pre-existing dev servers. Remaining design-system gaps are richer style management, richer theme/mode management, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused server, web, Rust, e2e runner, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm test`, `cargo test --workspace`, and `pnpm test:e2e` with 125 passing tests. |
| `2026-06-27-penpot-token-set-modes-baseline.md` | Completed | Added Penpot-inspired ordered token sets and active-set override resolution across renderer/server/Rust document contracts, DTCG import/export metadata, deterministic agent commands, MCP import surfaces, code export CSS-variable resolution, undo/redo-safe web state, and right Inspector token-set toggles. Remaining design-system gaps are reusable styles, richer theme/mode management, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused server, web, Rust, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm test`, `cargo test --workspace`, and `pnpm test:e2e` with 124 passing tests. |
| `2026-06-27-penpot-typography-token-baseline.md` | Completed | Added Penpot-inspired first-class typography tokens across renderer/server/Rust document contracts, DTCG import/export, deterministic agent and MCP commands, selected text Inspector binding controls, undo/redo-safe web state, persisted text bindings, and CSS-variable code export metadata. Remaining design-system gaps are token sets/modes, reusable styles, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused server, web, Rust, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm test`, and `pnpm test:e2e` with 123 passing tests. |
| `2026-06-16-rust-design-editor-foundation.md` | Completed, historical | Foundation commits are present in main: workspace scaffold, Rust document model, editor commands/context, Fastify/MCP server, renderer package, and Vite web shell. Current `pnpm test` and `pnpm typecheck` cover this baseline. |
| `2026-06-16-editor-mvp-interactions.md` | Completed, historical | Merged in PR #2, `9f48772`, with editor interaction coverage now in `apps/web/src/editor-state.test.ts`, server HTTP tests, Rust command tests, and Playwright editor e2e. |
| `2026-06-16-component-system-mvp.md` | Completed, historical | Merged in PR #3, `4a0e443`, with component definitions, component instances, detach support, HTTP/MCP surfaces, and Playwright coverage. |
| `2026-06-16-agent-control-mvp.md` | Completed, historical | Merged in PR #4, `8fb216b`, with `inspect_canvas`, `find_nodes`, `apply_agent_commands`, `validate_document`, `get_change_summary`, HTTP agent routes, and Playwright verification. |
| `2026-06-16-code-export-mvp.md` | Completed | Merged in PR #5, `feacf11`, with `export_code` and `GET /files/:fileId/export/code`. |
| `2026-06-16-component-abstraction-export.md` | Completed | Merged in PR #6, `82be355`, with structured `implementationSpec`, component references, implementation hints, and README documentation. |
| `2026-06-16-team-collaboration-foundation.md` | Completed, historical | Merged in PR #7, `277c2b9`, with the collaboration package, team store/session flow, TypeScript relay workflow, collaborative agent command path, and collaboration e2e coverage. |
| `2026-06-16-deployment-automation.md` | Completed, historical | Merged in PR #8, `3d824bc`, with static web deployment docs, relay Docker artifacts, and artifact tests. The GitHub Pages workflow from that slice was later removed from active CI. |
| `2026-06-16-collaboration-auth-permissions.md` | Completed, historical | Merged in PR #9, `87069a6`, with member roles, relay token hashes, owner/editor/viewer authorization, runtime credentials, and docs. Final verification boxes in the plan are stale; the current suite passed during the 2026-06-17 audit. |
| `2026-06-16-remote-cursors-selection.md` | Completed, historical | Merged in PR #10, `287b38b`, with presence session ids, cursor/selection bounds, overlay utilities, UI rendering, and collaboration e2e assertions. |
| `2026-06-17-team-manifest-management.md` | Completed | Merged in PR #11, `691aff6`, with manifest migration/validation, file import/export, URL import, UI status handling, and e2e coverage. |
| `2026-06-17-collaboration-e2ee.md` | Completed | Merged in PR #12, `03297dd`, with manifest encryption metadata, crypto helpers, encrypted relay mode, encrypted web provider, UI, docs, and e2e coverage. |
| `2026-06-17-rust-collab-relay.md` | Completed | Merged in PR #13, `b02fa10`, with Rust frame codec, auth/config, room hub, Axum runtime, scripts, tests, and docs. |
| `2026-06-17-web-only-laptop-layout.md` | Completed | Merged in PR #14, `a09a93e`, with laptop viewport overflow regression coverage and hidden input sizing fix. |
| `2026-06-17-figma-layout-foundation.md` | Completed | Implemented after the 2026-06-17 Figma feature audit with optional layout/constraints metadata, auto layout stacking, parent-resize constraints, inspector controls, MCP/HTTP agent command support, code-export metadata, and Playwright coverage. Verified with `pnpm test`, `pnpm typecheck`, `cargo test --workspace`, `pnpm --filter @layo/web build`, `pnpm test:e2e`, and `pnpm test:e2e:collab`. |
| `2026-06-17-mcp-ai-editing-verification.md` | Completed | Added MCP client integration coverage using `InMemoryTransport`, verified `inspect_canvas`, `apply_agent_commands`, `find_nodes`, and `validate_document`, added read/write tool annotations for AI clients, and added Playwright CLI coverage for stdio MCP node move, component creation, instance creation, and visible editor inspector state. `test:e2e` now runs shared local-file e2e specs with `--workers=1` to avoid sample-file reset races. Verified with `pnpm --filter @layo/server test -- src/mcp.test.ts`, `pnpm exec playwright test apps/web/e2e/mcp-stdio.spec.ts --reporter=line`, `pnpm test:e2e`, `pnpm typecheck`, `pnpm test`, and `pnpm --filter @layo/web build`. |
| `2026-06-17-multi-select-area-selection.md` | Completed | Added Figma-like Shift-click multi-selection and drag 영역 선택 with `selection.nodeIds`, primary `selection.nodeId` compatibility, multi-selection inspector state, layer/canvas selection outlines, and DOM area-selection overlay for direct browser verification. Verified with `pnpm --filter @layo/web test -- src/editor-state.test.ts`, `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "Figma-like multi-selection" --reporter=line`, full Playwright/TypeScript/build/test suites, and direct live UI interaction verification. |
| `2026-06-17-project-manifest-storage.md` | Completed | Added `ProjectManifest` filesystem persistence, default sample-project migration, project HTTP routes, MCP project tools, web project switch/create/rename/share workflow, CORS preflight support for browser JSON mutations, README docs, and Playwright coverage for creating, reopening, and team-linking a project. Verified with focused server/web tests, focused Playwright CLI, `pnpm test`, `pnpm typecheck`, `cargo test --workspace`, `pnpm --filter @layo/web build`, and `pnpm test:e2e`. |
| `2026-06-18-alignment-distribution.md` | Completed | Added Korean toolbar controls for selected-layer alignment and distribution, backed by `selection.nodeIds` batch geometry commands, undo/redo-safe state helpers, state/e2e tests, and Figma interaction rule documentation. Verified with `git diff --check`, `pnpm --filter @layo/web test -- src/editor-state.test.ts`, `pnpm test:e2e`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e:collab`, and direct headed Playwright CLI verification. |
| `2026-06-18-multi-node-drag-snap.md` | Completed | Added grouped selected-layer drag using batch geometry commands, transient snap guide overlays during drag, page-level snap target calculation, state/e2e coverage, and direct headed Playwright CLI verification. Verified with `git diff --check`, `pnpm --filter @layo/web test -- src/editor-state.test.ts --runInBand`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test:e2e`, `pnpm test`, `cargo test --workspace`, `pnpm test:e2e:collab`, and `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "multi-selection drags together" --headed --workers=1 --reporter=line`. |
| `2026-06-18-image-paste-drop-import.md` | Completed | Added local image asset storage, `image` node creation, browser clipboard paste and file drag/drop insertion, persisted document node creation, Konva image rendering, product docs separating landed image insertion from later full Figma import, and Playwright coverage. Verified with baseline `pnpm test`, `git diff --check`, focused server/web tests, focused and headed image Playwright CLI tests, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-19-collaboration-concurrency-safeguards.md` | Completed | Added granular Yjs field-level merge coverage, page-id merge safeguards for independently seeded collaborators, soft editing awareness claims, team asset metadata contracts, collaborative agent stale-state guard, encrypted Yjs sync-envelope recovery, and two-browser concurrency e2e coverage. Verified with focused red/green tests, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, and `pnpm test:e2e:collab` repeated after the encrypted order-dependent failure was fixed. |
| `2026-06-22-object-copy-paste-shortcuts.md` | Completed | Added single selected-object Cmd/Ctrl+C and Cmd/Ctrl+V through an in-app session clipboard, deterministic copied ids/names, 24 px paste offsets, undoable create-node command behavior, focused state/e2e coverage, product docs, and headed Playwright CLI verification. Verified with focused RED/GREEN tests, `git diff --check`, `pnpm --dir apps/web exec vitest run src/editor-state.test.ts`, focused and headed Playwright object clipboard tests, `pnpm test:e2e`, `pnpm typecheck`, `pnpm --filter @layo/web build`, and `pnpm test`. |
| `2026-06-22-edge-resize-handles.md` | Completed | Added top/right/bottom/left edge resize handles alongside the existing corners, edge-specific one-axis resizing, stable hit priority between edge and corner handles, explicit bottom-right e2e targeting for the older MVP resize path, and deterministic encrypted-provider coverage that separates initial sync frames from the actual granular update frame. Verified with RED/GREEN edge Playwright coverage, `git diff --check`, focused state tests, focused and headed resize Playwright tests, `pnpm test:e2e`, `pnpm typecheck`, `pnpm --filter @layo/web build`, and `pnpm test`. |
| `2026-06-22-multi-selection-group-affordance.md` | Completed | Added a combined multi-selection group outline and combined dimensions badge while keeping resize handles single-selection only. Verified with RED/GREEN Playwright coverage, `git diff --check`, `pnpm run check:design-rules`, focused state/e2e tests, `pnpm typecheck`, `pnpm test:e2e`, `pnpm --filter @layo/web build`, `pnpm test`, and direct headed Playwright CLI verification. |
| `2026-06-22-inspector-alignment-affordance.md` | Completed | Separated Inspector align and distribution controls into distinct groups, added Korean tooltips, made disabled distribution state explicit, and stabilized encrypted-provider frame-forwarding tests found during verification. Verified with focused RED/GREEN Playwright coverage, `git diff --check`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test:e2e`, web build, headed Playwright CLI verification, focused web unit tests, and `pnpm test`. |
| `2026-06-24-penpot-flex-alignment.md` | Completed | Added Penpot Flex-like `align_items` and `justify_content` to the shared layout model, web/server layout solvers, MCP schema defaults, Rust JSON model with legacy-layout serde defaults, code-export metadata, Korean Inspector controls, e2e coverage, and Penpot maturity docs. Verified with focused RED/GREEN tests, `pnpm run check:penpot-maturity`, focused web/server/Rust tests, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `cargo test --workspace`, focused Playwright CLI e2e, and Playwright API-log direct UI interaction proof. |
| `2026-06-24-penpot-flex-wrap.md` | Completed | Added Penpot-like Flex `wrap` and `align_content` across shared node types, web/server layout solvers, MCP schema, Rust serialization and generated bindings, Korean Inspector controls, code-export coverage, and Playwright CLI proof. Verified with focused RED/GREEN tests, `cargo test -p editor-core`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `cargo test --workspace`, `git diff --check`, focused Playwright CLI, Playwright API-log proof, and `pnpm test:e2e`. |
| `2026-06-24-penpot-layout-item-position.md` | Completed | Added Penpot-like static/absolute child layout-item positioning across shared node types, web/server layout solvers, MCP schema, Rust serialization and generated bindings, Korean Inspector controls, code-export coverage, and Playwright CLI coverage. Verified with focused RED/GREEN tests, `pnpm run check:penpot-maturity`, focused web/server/Rust tests, `cargo test -p editor-core`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `cargo test --workspace`, `git diff --check`, focused Playwright CLI, Playwright API-log proof, and `pnpm test:e2e`. |
| `2026-06-24-penpot-layout-item-margin.md` | Completed | Added Penpot-like per-child `layout_item.margin` to the shared node model, web/server auto-layout solvers, MCP/HTTP agent command surfaces, code export, Rust serialization and generated bindings, Korean Inspector controls, and Playwright CLI coverage. Verified with focused web/server/Rust tests, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `cargo test --workspace`, focused Playwright CLI, Playwright API-log proof, and `pnpm test:e2e`. |
| `2026-06-24-penpot-row-column-gap.md` | Completed | Added Penpot-like Flex row/column gap split across shared node types, web/server layout solvers, MCP schema, Rust serialization and generated bindings, code export, Korean Inspector controls, and Playwright CLI coverage. Verified with focused RED/GREEN web/server/Rust tests, `cargo test -p editor-core`, focused Playwright CLI, and Playwright API-log proof. Full-suite verification is recorded in the implementation plan and PR. |
| `2026-06-25-penpot-flex-fit-sizing.md` | Completed | Added Penpot-like Flex `fit` width/height sizing for auto-layout containers across shared node types, web/server layout solvers, MCP schema, Rust serialization and generated bindings, code export, Korean Inspector controls, and Playwright CLI coverage. Ponytail was adopted as a process guard through the existing minimal-change ladder, not as a runtime dependency. Verified with focused RED/GREEN web/server/Rust tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `git diff --check`, focused Playwright CLI, and `pnpm test:e2e`. |
| `2026-06-25-penpot-layout-item-fill-sizing.md` | Completed | Added Penpot-like `layout_item.width_sizing` and `layout_item.height_sizing` fill behavior for children inside fixed auto-layout containers across shared node types, web/server layout solvers, MCP schema, Rust serialization and generated bindings, code export, Korean Inspector controls, and Playwright CLI coverage. Fill is inert on circular `fit` parent axes. Verified with focused RED/GREEN web/server/Rust tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `git diff --check`, focused Playwright CLI, and `pnpm test:e2e`. |
| `2026-06-25-penpot-grid-auto-layout.md` | Completed | Added Penpot-like Grid `layout.mode = grid` with equal-cell auto-placement, grid row/column counts, row/column gaps, padding, static/absolute item flow behavior, shared web/server/Rust contracts, MCP schema, code export, Korean Inspector controls, and Playwright CLI coverage. Manual grid cell placement, named areas, and FR/auto/pixel track units remain later maturity gaps. Verified with focused RED/GREEN web/server/Rust tests, `pnpm typecheck`, `cargo test -p editor-core`, focused Playwright CLI, and full-suite checks recorded in the implementation plan and PR. |
| `2026-06-25-penpot-grid-manual-placement.md` | Completed | Added Penpot-like Grid manual one-cell placement with `layout_item.grid_column` and `layout_item.grid_row` across shared web/server/Rust contracts, MCP schema, code export, Korean Inspector controls, and Playwright CLI coverage. Grid spans, named areas, viewport area editing, and FR/auto/pixel track units remain later maturity gaps. Verified with focused RED/GREEN web/server/Rust tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `git diff --check`, focused Playwright CLI, and `pnpm test:e2e`. |
| `2026-06-25-penpot-grid-span.md` | Completed | Added Penpot-like Grid item spans with `layout_item.grid_column_span` and `layout_item.grid_row_span` across shared web/server/Rust contracts, MCP schema, code export, Korean Inspector controls, and Playwright CLI coverage. Named grid areas, viewport area editing, and non-equal track units remain later maturity gaps. Verified with RED/GREEN web/server/Rust tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, focused Playwright CLI, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm --filter @layo/collaboration build`, `git diff --check`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-26-penpot-grid-track-units.md` | Completed | Added Penpot-like Grid pixel/fraction/auto track units with `layout.grid_column_tracks` and `layout.grid_row_tracks` across shared web/server/Rust contracts, MCP schema, generated bindings, Korean Inspector controls, and Playwright CLI coverage. Named grid areas, baseline alignment, viewport grid editing, and deeper resizing semantics remained later maturity gaps at the time of that PR. Verified with focused RED/GREEN web/server/Rust tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, focused Playwright CLI, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `git diff --check`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-26-penpot-grid-areas.md` | Completed | Added Penpot-like named Grid areas with `layout.grid_areas` and `layout_item.grid_area` across shared web/server/Rust contracts, MCP schema, generated bindings, Korean Inspector controls, and Playwright CLI coverage. Baseline alignment, viewport grid editing, and deeper resizing semantics remain later maturity gaps. Verified with focused RED/GREEN web/server/Rust tests, web/server typechecks, focused Playwright CLI, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `git diff --check`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-26-penpot-layout-min-max-sizing.md` | Completed | Added Penpot-like min/max sizing rules with `layout.min_width/max_width/min_height/max_height` and matching `layout_item` fields across shared web/server/Rust contracts, MCP schema, generated bindings, Korean Inspector controls, and Playwright CLI coverage. Baseline alignment, viewport grid editing, and deeper resizing semantics remain later maturity gaps. Verified with focused RED/GREEN web/server/Rust tests, web/server typechecks, focused Playwright CLI, and `cargo test -p editor-core`; full-suite verification is recorded in the implementation plan and PR. |
| `2026-06-26-penpot-flex-reverse-direction.md` | Completed | Added Penpot-like reverse row and reverse column flow with `horizontal_reverse` and `vertical_reverse` across shared web/server/Rust contracts, MCP schema, generated bindings, web/server layout solvers, Korean Inspector controls, and Playwright CLI coverage. Baseline alignment, viewport grid editing, and deeper resizing semantics remain later maturity gaps. Verified with focused RED/GREEN web/server/Rust tests and focused Playwright CLI; full-suite verification is recorded in the implementation plan and PR. |
| `2026-06-26-penpot-grid-viewport-editing.md` | Completed | Added Penpot-like viewport Grid track resize handles for selected grid frames and components. Canvas column/row handles materialize dragged tracks as `px` values through the existing `set_node_layout` command path, preserving relayout, undo/redo, persistence, and collaboration command semantics. Viewport add/remove controls, viewport area/span editing, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN focused Playwright CLI, web typecheck, and full-suite verification recorded in the implementation plan and PR. |
| `2026-06-26-penpot-grid-viewport-add-controls.md` | Completed | Added Penpot-like selected-grid viewport `+` controls for appending rows and columns. The controls append `1fr` tracks through the existing `set_node_layout` path, preserving Inspector values, relayout, undo/redo, persistence, and collaboration command semantics. Viewport remove/reorder controls, row/column context menus, viewport area/span editing, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN focused Playwright CLI, Playwright API-log interaction proof, web typecheck, Penpot maturity guard, web build, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-viewport-remove-controls.md` | Completed | Added Penpot-like selected-grid viewport `-` controls for deleting specific rows and columns. The controls filter the selected track through the existing `set_node_layout` path, preserving Inspector values, relayout, undo/redo, persistence, and collaboration command semantics. Viewport reorder controls, row/column context menus, delete-with-shapes variants, viewport area/span editing, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN focused Playwright CLI; full-suite verification is recorded in the implementation plan and PR. |
| `2026-06-26-penpot-grid-header-context-menu.md` | Completed | Added Penpot-like selected-grid row/column header context menus for inserting before/after, duplicating, and deleting tracks. The actions mutate `grid_column_tracks` and `grid_row_tracks` through the existing `set_node_layout` path, preserving Inspector values, relayout, undo/redo, persistence, and collaboration command semantics. Viewport reorder controls, delete-with-shapes variants, viewport area/span editing, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN focused Playwright CLI, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-delete-track-shapes.md` | Completed | Added Penpot-like selected-grid row/column header menu actions for deleting a track and the static grid-flow shapes placed in that track. The command removes affected unlocked children, preserves absolute children out of flow, blocks locked affected children, relayouts remaining children, and restores the prior frame snapshot through undo/redo. Viewport reorder controls, viewport area/span editing, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN unit and Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-reorder-tracks.md` | Completed | Added Penpot-like selected-grid viewport row/column header drag reordering. The command reorders the selected track, materializes static grid-flow child placements so objects move with their tracks, blocks locked moving children, and restores the prior frame snapshot through undo/redo. Area/span-aware reorder semantics, Penpot's leave-elements-in-place reorder variation, viewport area/span editing, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN unit and Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-ctrl-preserve-reorder.md` | Completed | Added Penpot-like selected-grid Ctrl/Command row/column header drag reordering that moves track order while preserving static grid-flow object x/y positions. The command materializes explicit grid placement and margins so children remain static grid items, preserves redo semantics through the undo inverse, and suppresses Ctrl-drag context menu leakage. Area/span-aware viewport reorder semantics, viewport area/span editing, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN unit and Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-span-area-reorder.md` | Completed | Added Penpot-like selected-grid viewport row/column reorder handling for spanned grid items and named grid areas. The command remaps every covered track index through the move, materializes the smallest contiguous bounding span for explicit children, moves named area definitions the same way, and preserves child `grid_area` bindings. Direct viewport area/span boundary editing, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN unit and Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-area-boundary-handles.md` | Completed | Added Penpot-like selected grid item/area boundary handles. Selected auto or explicit grid children show four viewport handles that materialize explicit `layout_item` placement on drag, and selected named-area children update the parent `grid_areas` definition while preserving child `grid_area` selection. Empty-cell merge menus, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-empty-cell-merge-menu.md` | Completed | Added the first Penpot-like selected-grid empty-cell context menu. Selected grid frames render per-cell hit zones, right-click opens a cell menu, and `셀 병합 영역 만들기` appends a deterministic named `grid_areas` entry through `set_node_layout`, preserving existing undo/redo, persistence, and collaboration command semantics. Multi-cell merge/split menus, baseline alignment, and deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN Playwright CLI coverage, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-26-penpot-grid-multicell-merge-split.md` | Completed | Added Penpot-like selected-grid multi-cell area merge and split menus. Ctrl/Command-click cell ranges show `grid-cell-selection-range`, right-clicking the range creates a spanned named `grid_areas` entry through `set_node_layout`, and right-clicking an existing named area exposes `병합 영역 분리` to remove it. Baseline alignment and deeper resizing semantics remain later layout maturity gaps. Verified with RED/GREEN focused Playwright CLI, Playwright API-log interaction proof, `pnpm --filter @layo/web typecheck`, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |

| `2026-06-27-dev-handoff-panel.md` | Completed | Added a visible Inspector `개발` tab backed by the existing `/files/:fileId/export/code` HTTP export. The panel shows selected-layer id/name/kind, geometry/fill specs, generated CSS, generated HTML, and exported structure snippets, and the existing code action now switches to that tab. Web API helpers expose typed code export data for UI use. Remaining developer-handoff gaps are copy controls, SVG/asset downloads, richer ready-for-dev annotations, webhooks/API integration stories, and repo component mappings. Verified with RED/GREEN web API helper coverage, RED/GREEN Playwright CLI coverage, focused web tests, typecheck, and the Penpot maturity gate. |
| `2026-06-27-shared-library-archive.md` | Completed | Added package-style `.layo-library.zip` export/review/import for reusable components, tokens, and component image assets. Storage validates archive manifests/assets and remaps conflicting token/component ids, HTTP exposes file-scoped library export/review/import routes, web API helpers cover request shapes, and the Korean file panel can export, review, prefix, and merge a library into the current file. Hosted registry, publish/subscribe, update notifications, and live library sync remain later design-system gaps. Verified with RED/GREEN storage, HTTP, web API, focused Playwright CLI, `pnpm --filter @layo/server test -- src/storage.test.ts src/http.test.ts`, `pnpm --filter @layo/web test -- src/document-api.test.ts`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-project-archive-import-export.md` | Completed | Added Layo project-level ZIP archive export/review/import for `ProjectManifest`, all project documents, and referenced image assets through storage, HTTP, web API helpers, and Korean file-panel controls. Imports create fresh private projects and fresh document ids to avoid overwriting existing work or preserving stale sharing state. Verified with RED/GREEN storage, HTTP, web API, and Playwright CLI coverage plus maturity gates. |
| `2026-06-27-file-archive-web-import-review.md` | Completed | Added file-panel controls for exporting the current document as a `.layo.zip`, selecting an archive, showing a server-validated import review before writes, and importing the reviewed archive into a fresh project shell. Server storage/HTTP now expose no-write archive review, web API helpers cover review/import/export request shapes, and Playwright CLI proves create project -> export archive -> review upload -> confirm import -> open imported project. Multi-file project archives, shared library packaging, backup/restore runbooks, and Penpot/Figma migration remain later import/export maturity gaps. Verified with RED/GREEN focused server/web API tests, focused Playwright CLI, web/server typechecks, Penpot maturity guard, and build/test gates recorded in the plan. |
| `2026-06-27-file-archive-import-export.md` | Completed | Added a Penpot-inspired Layo file archive ZIP format with readable `manifest.json`, readable `document.json`, and packaged referenced image asset metadata/bytes. Storage can export/import archives while preserving image asset ids, HTTP exposes `GET /files/:fileId/export/archive` and `POST /files/import/archive`, and MCP exposes `export_file_archive`/`import_file_archive` for agent handoff. Multi-file project archives, shared library packaging, backup/restore runbooks, and Penpot/Figma migration remain later import/export maturity gaps. Verified with RED/GREEN focused file-archive, storage, HTTP, and MCP coverage; broad-suite verification is recorded in the implementation plan. |
| `2026-06-27-penpot-comment-event-stream.md` | Completed | Added process-local SSE comment event delivery for open editor sessions. The server exposes `GET /comments/events`, publishes compact create/reply/resolve/read comment mutation events after successful storage writes, and includes explicit CORS headers on the hijacked stream response. The web API exposes `subscribeToCommentEvents`, the App refreshes current-file comments/notifications/activity on matching stream events while keeping polling fallback, and Playwright CLI disables the polling interval to prove external comments update through EventSource. Verified with RED/GREEN server, web API, and browser coverage plus `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-live-comment-notifications.md` | Completed | Added browser-delivered local comment notification/activity refresh for open editor sessions. The web app now periodically refreshes current-file comment threads, project/file notification summaries, and retained activity so comments created through another browser, HTTP client, MCP client, or agent appear in the file panel without reload. Storage-generated comment timestamps are now monotonic to keep latest-activity notification ordering deterministic under fast writes. Verified with RED/GREEN Playwright CLI coverage, focused storage regression coverage, `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-team-mention-targets.md` | Completed | Added Penpot-inspired structured local team-member mention targets for selected-node comment threads, replies, and retained activity events. Storage persists `mentionTargets`, HTTP and MCP create/reply surfaces accept explicit targets, web API helpers expose typed target arrays, and the Inspector resolves active team manifest members from `@displayName` or `@userId` into `팀 멘션` chips while preserving unresolved legacy `언급` chips. Verified with RED/GREEN focused storage, HTTP, MCP, web API typecheck, and Playwright CLI coverage, `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comment-activity-feed.md` | Completed | Added a Penpot-inspired retained local comment activity feed for create, reply, and resolve events. Comment sidecars now retain bounded recent activity, storage exposes `listCommentActivity`, HTTP exposes `GET /comments/activity`, MCP exposes `list_comment_activity`, web API helpers expose `listCommentActivity`, and the file panel shows the current project's recent comment activity. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI tests; full-suite verification is recorded in the implementation plan. |
| `2026-06-27-penpot-mention-notification-inbox.md` | Completed | Added Penpot-inspired local viewer-targeted mention notification counts to project/file comment summaries and the file panel. Storage now counts unresolved unread threads that mention the viewer, sorts notification rows by latest unread comment activity, HTTP/MCP/web API return `totalMentions` and per-project/file `mentionCount`, and the file panel shows `나를 멘션 N개` plus per-file mention counts. Verified with RED/GREEN focused storage sort/mention tests, HTTP/MCP/web API tests, Playwright CLI focused file-panel coverage, `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comment-notification-summary.md` | Completed | Added Penpot-inspired local project/file unread comment notification summaries and current-file mark-read. Storage aggregates unread active comment threads by project document membership, HTTP exposes `GET /comments/notifications` and `POST /files/:fileId/comments/read`, MCP exposes `list_comment_notifications` and `mark_file_comments_read`, web API helpers expose `listCommentNotifications` and `markFileCommentsRead`, and the file panel shows unread counts plus `현재 파일 읽음`. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI tests, `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comment-mentions-unread.md` | Completed | Added Penpot-inspired persisted `@mention` extraction and local viewer unread/read state to selected-node comment threads and replies. Storage now persists `mentions` and `readBy`, HTTP supports `viewerId` listing and `/read`, MCP supports `viewerId` and `mark_comment_thread_read`, web API helpers expose `markCommentThreadRead`, and the Inspector shows mention chips, unread badges, and `읽음 처리`. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI tests, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/server typecheck`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comment-replies.md` | Completed | Added Penpot-inspired persisted replies to selected-node comment threads through sidecar storage, `POST /files/:fileId/comments/:threadId/replies`, MCP `add_comment_reply`, web API helpers, and the Inspector reply composer. Verified with RED/GREEN focused server/web API tests, focused Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts -g "comments panel adds replies to a selected-layer thread" --workers=1 --reporter=line`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/server typecheck`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comment-bubbles.md` | Completed | Added Penpot-inspired viewport bubbles for unresolved selected-node comments. Bubbles are anchored from document geometry, show unresolved counts, select the commented node on click, and disappear after the final active thread is resolved. Verified with RED/GREEN focused Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts -g "canvas comment bubbles open selected-layer threads and disappear after resolve" --workers=1 --reporter=line`, `pnpm --filter @layo/web typecheck`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-file-version-retention.md` | Completed | Added Penpot-inspired saved-version delete and explicit retention cleanup. Storage, HTTP, MCP, web API helpers, and the file panel can delete a saved version, including pinned versions when explicitly requested, while retention pruning keeps pinned checkpoints and only removes old unpinned versions. Full visual preview and branch/review/merge remain later team-product gaps. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI coverage, `git diff --check`, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/server typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-comments-foundation.md` | Completed | Added the first Penpot-inspired selected-node comment foundation. Comments persist outside `DesignFile` in `.layo/comments/<fileId>.json`, expose HTTP create/list/resolve routes, MCP `create_comment_thread`/`list_comment_threads`/`resolve_comment_thread` tools, web API helpers, and a Korean-first right Inspector panel for selected objects. Replies, mentions, unread notifications, live comment sync, and dashboard notifications remain later team-product gaps. Verified with RED/GREEN `pnpm --filter @layo/server test -- src/storage.test.ts -t "comment threads"`, RED/GREEN `pnpm --filter @layo/server test -- src/http.test.ts src/mcp.test.ts -t "comment"`, RED/GREEN `pnpm --filter @layo/web test -- src/document-api.test.ts -t "comment"`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "comments panel creates and resolves" --workers=1 --reporter=line`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/server typecheck`, `pnpm --filter @layo/web build`, `pnpm run check:penpot-maturity`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-version-preview-diff.md` | Completed | Added Penpot-inspired saved-version preview/diff in the file panel. Users can click `미리보기` for a saved manual/automatic version, see current-file comparison counts and changed node ids from the existing change-summary route, close the preview, or restore from the preview. Server change summaries now compare node bodies without double-counting parent containers when only descendants change. Full view-only canvas preview, pinned/retention controls, comments, and branch/review/merge workflows remain later team-product gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/document-api.test.ts -t "summarizes document changes"`, RED/GREEN `pnpm --filter @layo/server test -- src/storage.test.ts -t "changed descendants"`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "file version history previews saved version differences before restore" --workers=1 --reporter=line`, focused file-version Playwright CLI, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/server typecheck`, `pnpm --filter @layo/web build`, `pnpm run check:penpot-maturity`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-pinned-file-versions.md` | Completed | Added Penpot-inspired pinned saved-version checkpoints. Version sidecars now persist `pinned`, legacy snapshots default to unpinned, pinned versions sort first, and storage/HTTP/MCP/web API/file-panel controls support pin and unpin. Retention/delete policy, full visual preview, and branch/review/merge remain later team-product gaps. Verified with RED/GREEN focused storage, HTTP, MCP, web API, and Playwright CLI tests, plus full-suite verification recorded in the implementation plan. |
| `2026-06-27-penpot-auto-file-history.md` | Completed | Added Penpot-inspired automatic file versions after persisted saved edits, with separate `.layo/history-state/<fileId>.json` counters, thresholded `source: "auto"` snapshots, HTTP/MCP list/read visibility, and a file-panel `자동 저장` label. Comments, visual diff, pinned/version preview controls, branch/review/merge, and richer recovery workflows remain later team-product gaps. Verified with focused RED/GREEN storage, HTTP, MCP, and Playwright CLI tests, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm test`, and `pnpm test:e2e`. |
| `2026-06-27-penpot-file-version-history.md` | Completed | Added Penpot-inspired manual saved file versions with immutable local-first `.layo/history/<fileId>/<versionId>.json` snapshots, HTTP and MCP list/save/read/restore surfaces, a Korean-first file-panel history UI, restore recovery snapshots, and persisted inspector text edits so restore is proven against saved file state rather than transient UI state. Automatic edit history, visual diff, and branch/review workflows remain later team-product gaps. Verified with RED/GREEN focused server/web tests, focused Playwright CLI, Playwright API-log direct interaction proof, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-spacing-token-bindings.md` | Completed | Added Penpot-inspired spacing tokens, DTCG spacing/dimension import-export, saved layout gap/padding token bindings, Inspector controls, MCP/HTTP agent surfaces, and CSS-variable code export. Playwright CLI exposed and then verified the missing `layout: none` persistence case, so token binding now promotes frames to auto layout and persists through `set_layout`. Verified with focused server/web/Rust tests, focused Playwright CLI, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-dtcg-token-import-export.md` | Completed | Added Penpot/DTCG-inspired document-local color-token import/export through a focused server codec, persisted storage methods, `GET`/`PUT /files/:fileId/tokens/dtcg`, MCP `export_design_tokens`/`import_design_tokens`, and a Korean-first right Inspector token JSON panel. Token sets/modes, typography/spacing tokens, reusable styles, variants, shared libraries, and code mappings remain later design-system maturity gaps. Verified with RED/GREEN server codec/storage, HTTP, MCP, and Playwright CLI e2e coverage. |
| `2026-06-27-penpot-color-token-binding.md` | Completed | Added first-class document color tokens and node `style.fill_token` bindings across renderer/server/Rust contracts, deterministic agent commands, MCP schema, validation, Inspector token readout, undoable web command handling, and CSS-variable-backed code export metadata. Token sets/modes, W3C/DTCG import-export, typography/spacing tokens, reusable styles, variants, shared libraries, and code mappings remain later design-system maturity gaps. Verified with focused RED/GREEN server/export/Rust/web tests, focused Playwright CLI, web/server/full typechecks, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-layout-direct-resize-sizing.md` | Completed | Added Penpot-like direct resize semantics for fill layout children so manually resized axes become fixed instead of snapping back during relayout. Undo/redo restores the previous `layout_item`, and server geometry updates apply the same saved-document rule. Verified with focused RED/GREEN web state and server storage tests, focused Playwright CLI coverage, web/server typechecks, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-flex-baseline-alignment.md` | Completed | Added Penpot/CSS-inspired `align_items: baseline` for horizontal and horizontal-reverse auto-layout rows across web/server layout solvers, MCP schema, Rust JSON model, code export, Inspector controls, and Playwright CLI coverage. Vertical, wrapped-line, and grid baseline alignment remain later maturity gaps. Verified with focused RED/GREEN web state, server storage, Rust model, and code-export tests, focused Playwright CLI, web/server/full typechecks, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-flex-wrap-baseline.md` | Completed | Added Penpot/CSS-inspired baseline alignment inside each horizontal wrapped flex line across web/server layout solvers, server agent commands, and Playwright CLI coverage. Vertical and grid baseline alignment remain later maturity gaps. Verified with RED/GREEN web state and server storage tests, focused Playwright CLI, web/server/full typechecks, `pnpm run check:penpot-maturity`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-penpot-grid-baseline-alignment.md` | Completed | Added Penpot/CSS-inspired row baseline alignment for grid children across web/server grid solvers, server agent commands, and Inspector Playwright CLI coverage. Vertical writing-mode baseline alignment, deeper CSS baseline group semantics, and remaining deeper resizing semantics remain later maturity gaps. Verified with RED/GREEN web state and server storage tests plus focused Playwright CLI; broad verification is recorded in the implementation plan and PR. |
| `2026-06-27-penpot-text-writing-mode-baseline.md` | Completed | Added the first Penpot/CSS-inspired vertical text writing-mode slice. Text nodes can persist `writing_mode`, the right Inspector exposes writing direction for selected text, web/server baseline layout uses vertical text box width for baseline offset, server agent commands and MCP create-text understand writing mode, and code export emits structure `writingMode` plus CSS `writing-mode`. Full visual vertical text rendering, glyph orientation controls, and deeper CSS baseline group semantics remain later maturity gaps. Verified with RED/GREEN web state, server export/storage, Rust round-trip, focused Playwright CLI, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 122 passing tests, and `git diff --check`. |
| `2026-06-27-penpot-grid-item-self-alignment.md` | Completed | Added Penpot-like grid item `justify_self` and `align_self` controls across renderer/server/Rust layout contracts, MCP `set_layout_item`, web/server grid solvers, code export metadata, and the selected-grid-child inspector. Verified with RED/GREEN web/server/Rust/export tests, focused Playwright CLI, Playwright API-log interaction proof, `pnpm typecheck`, `pnpm run check:penpot-maturity`, `cargo test -p editor-core`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. Baseline alignment and deeper resizing semantics remain later maturity gaps. |
| `2026-06-26-penpot-grid-justify-items-stretch.md` | Completed | Added Penpot-like container-level grid horizontal item alignment with `justify_items: stretch`. The field is now in renderer/server/Rust layout contracts, MCP `set_layout`, web/server grid solvers, structured export metadata, and a grid-only inspector select. Verified with RED/GREEN web and server tests, Rust JSON round-trip/default tests, focused Playwright CLI, and Playwright API-log interaction proof. Baseline alignment and deeper resizing semantics remain later maturity gaps. |
| `2026-06-27-vercel-production-ci.md` | Completed | Added a repository-owned Vercel production deployment workflow using required secret checks, pinned pnpm and `vercel@50.9.6`, `vercel pull --yes --environment=production`, `vercel build --prod`, `vercel deploy --prebuilt --prod`, and a post-deploy live smoke gate. Later deployment URL-source hardening moved this workflow away from the stale `https://layo.vercel.app` assumption. Local repository gates pass; manual `jsleemasters-projects/layo` production is verified at `https://layo-three.vercel.app/`; workflow-owned production remains unproven until repository Vercel secrets exist and the workflow/live smoke passes. |
| `2026-06-27-vercel-production-smoke.md` | Completed | Added a live Vercel production smoke gate so a public URL is not treated as deployed unless it serves the Layo Vite shell marker and same-origin `/health`. The repo now has `pnpm run check:live-deployment -- --url <url>` plus mock-driven verifier tests and deployment docs. The stale `https://layo.vercel.app` live check still fails because it serves Next.js shell assets; the verified manual production URL is `https://layo-three.vercel.app/`, owned by `jsleemasters-projects/layo`. |
| `2026-06-27-vercel-deploy-url-source.md` | Completed | Hardened the Vercel production workflow after the stale-host failure. The workflow now captures the URL returned by `vercel deploy --prebuilt --prod`, smoke-tests that returned URL, and updates GitHub About only after smoke passes using `LAYO_REPOSITORY_ADMIN_TOKEN`. The live smoke script no longer defaults to `https://layo.vercel.app`; missing URLs fail before network access. Manual production is verified at `https://layo-three.vercel.app/`; workflow-owned production still requires Vercel and repository-admin secrets plus a workflow-run URL that passes live smoke. Verified with RED/GREEN deployment artifact and smoke tests, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `git diff --check`, and explicit live guard failures for missing URL and the current stale `https://layo.vercel.app` host. |
| `2026-06-27-dev-panel-copy-controls.md` | Completed | Added Penpot-inspired copy controls to the visible Inspector Dev tab. Selected layers now expose CSS, HTML, and exported structure copy buttons backed by the existing server code export snippets, with clipboard status feedback in the Korean-first right Inspector. The Developer handoff benchmark now records copy controls as landed while SVG/asset downloads, richer annotations, webhooks/API stories, and repo component mappings remain open gaps. Verified with RED/GREEN Playwright CLI clipboard coverage, focused Dev panel e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e` with 103 passing tests. |
| `2026-06-27-dev-panel-svg-download.md` | Completed | Added a Penpot-inspired selected-layer SVG download action to the visible Inspector Dev tab. The web app now generates deterministic single-node SVG artifacts with node id/name metadata, text/shape fill, and selected-layer download status, while documenting that complex nested/image SVG fidelity and bitmap assets remain later developer-handoff gaps. Verified with RED/GREEN Playwright CLI download-content coverage, focused Dev panel e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e` with 104 passing tests. |
| `2026-06-27-dev-panel-png-download.md` | Completed | Added a Penpot-inspired selected-layer PNG download action to the visible Inspector Dev tab. The Dev panel now reuses the existing Konva selected-object crop/export path and verifies downloaded PNG signature bytes, while documenting that nested/image fidelity, export presets, scale options, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN Playwright CLI download-content coverage, focused Dev panel e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e` with 105 passing tests. |
| `2026-06-27-dev-panel-png-scale-options.md` | Completed | Added Penpot-inspired selected-layer PNG 1x/2x/3x scale options to the visible Inspector Dev tab. The default 2x path keeps the existing `text-1.png` filename and status, while explicit 1x/3x selections use scale-suffixed filenames and Playwright verifies the downloaded PNG IHDR dimensions grow at 3x. Nested/image fidelity, multi-format export presets beyond PNG scaling, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN focused Playwright CLI scale coverage, focused Dev panel and right-click PNG e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, and `pnpm test:e2e` with 106 passing tests. |
| `2026-06-27-dev-panel-jpeg-export.md` | Completed | Added Penpot-inspired selected-layer JPEG export to the visible Inspector Dev tab. The Dev panel now reuses the selected raster crop/export path for PNG and JPEG, preserves right-click/default PNG behavior, and verifies downloaded JPEG magic bytes. WEBP/PDF exports, persistent multi-export presets, nested/image SVG/PNG/JPEG fidelity, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN focused Playwright CLI JPEG coverage, focused Dev panel and right-click PNG e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 107 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-webp-export.md` | Completed | Added Penpot-inspired selected-layer WEBP export to the visible Inspector Dev tab. The Dev panel now reuses the selected raster crop/export path for PNG, JPEG, and WEBP, preserves right-click/default PNG behavior, and verifies downloaded WEBP RIFF signatures. PDF exports, persistent multi-export presets, nested/image SVG/PNG/JPEG/WEBP fidelity, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN focused Playwright CLI WEBP coverage, focused Dev panel and right-click PNG e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 108 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-pdf-export.md` | Completed | Added Penpot-inspired selected-layer PDF export to the visible Inspector Dev tab. The Dev panel now emits a deterministic single-page PDF Blob for selected text and shape layers with node title/id metadata and verified `%PDF-` structure. Persistent multi-export presets, nested/image SVG/PNG/JPEG/WEBP/PDF fidelity, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN focused Playwright CLI PDF coverage, focused Dev panel and right-click PNG e2e, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 109 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-export-presets.md` | Completed | Added Penpot-inspired persistent selected-layer export presets to the visible Inspector Dev tab and saved document model. Presets round-trip through Rust/renderer/server contracts, HTTP/MCP `set_export_presets`, undo/redo-safe web state, and the Dev panel can batch-download PNG/JPEG/WEBP/SVG/PDF artifacts with suffix-aware filenames. Multi-selection/page-level export review, zip packaging, nested/image export fidelity, annotations, webhooks/API, and repo mappings remain later developer-handoff gaps. Verified with RED/GREEN focused Rust, server/MCP, web state, and Playwright CLI coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, web/server/full typechecks, `pnpm --filter @layo/web build`, `pnpm test`, `cargo test --workspace`, `pnpm test:e2e` with 110 passing tests, `pnpm test:e2e:collab` with 5 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-multi-export-review.md` | Completed | Added Penpot-inspired multi-selection export review to the visible Inspector Dev tab. Selected layers with saved export presets now produce deterministic review rows, per-artifact include/exclude checkboxes, and checked-only downloads that reuse the existing node-local SVG/PDF and Konva raster export paths with explicit node ids. Page-level export review, zip packaging, nested/image export fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/export-presets.test.ts`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "inspector dev panel reviews multi-selection export presets before download" --workers=1 --reporter=line`, headed Playwright CLI interaction proof for the same flow, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 111 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-page-export-review.md` | Completed | Added Penpot-inspired page-level export review to the visible Inspector Dev tab. When no layer is selected, Layo now scans the current page tree for saved export presets, shows per-artifact include/exclude rows, and downloads checked artifacts through the same explicit-node raster/vector paths used by multi-selection review. Zip packaging, nested/image export fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/export-presets.test.ts`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "inspector dev panel reviews page export presets when no layer is selected" --workers=1 --reporter=line`, headed Playwright CLI proof for the same flow, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 112 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-export-zip.md` | Completed | Added Penpot-inspired export review ZIP packaging to the visible Inspector Dev tab. Checked page or multi-selection export review artifacts now download as a single browser-built ZIP archive using the existing explicit-node SVG/PDF/Konva raster export paths, while unchecked rows stay excluded. Complex nested/image export fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/zip-archive.test.ts`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "inspector dev panel reviews page export presets when no layer is selected" --workers=1 --reporter=line`, broad-failure GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "inspector dev panel reviews multi-selection export presets before download" --workers=1 --reporter=line`, headed Playwright CLI proof for the page ZIP flow, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 112 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-nested-export.md` | Completed | Added nested child-layer fidelity for selected frame/group SVG and PDF artifacts in the visible Inspector Dev tab. The App-local vector/PDF helpers are now a tested `node-artifacts` module that recursively emits nested text and shape children with parent-relative transforms, and Playwright CLI verifies downloaded `frame-1.svg`/`frame-1.pdf` contain the nested `text-1` content. Image asset embedding in SVG/PDF, raster/effect export fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/node-artifacts.test.ts`, focused Dev panel export Playwright CLI, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web typecheck`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 113 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-image-asset-export.md` | Completed | Added Penpot-inspired source image preservation for selected-layer SVG/PDF artifacts in the visible Inspector Dev tab. SVG image nodes now export as embedded data-URL `<image>` elements, PDF artifacts embed source image bytes through `/EmbeddedFiles`, JPEG assets can render as PDF image XObjects, and browser downloads fetch stored `/assets/:assetId` bytes before generating artifacts. Non-JPEG PDF visual image rendering, raster/effect fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/node-artifacts.test.ts`, focused Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "image asset bytes" --reporter=line`, plus full-suite verification recorded in the implementation plan. |
| `2026-06-27-dev-panel-png-pdf-rendering.md` | Completed | Added visible PNG image rendering for selected-layer PDF artifacts. The web artifact helper now decodes non-interlaced 8-bit PNG scanlines with `fflate`, creates `/Filter /FlateDecode` RGB image XObjects, adds `/SMask` soft masks when PNG alpha exists, and still embeds the original source PNG through `/EmbeddedFiles`. WEBP PDF visual rendering, broader raster/effect fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/node-artifacts.test.ts` and focused Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "image asset bytes" --reporter=line`; broad verification is recorded in the implementation plan. |
| `2026-06-27-dev-panel-webp-pdf-rendering.md` | Completed | Added visible WEBP image rendering for selected-layer PDF artifacts. The browser artifact loader now converts fetched WEBP assets into PDF-only PNG previews through native image decode and canvas, while `pdfForNode` preserves the original WEBP through `/EmbeddedFiles` and renders the preview as a `/Filter /FlateDecode` image XObject. Broader raster/effect fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/web test -- src/node-artifacts.test.ts`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "webp image artifacts" --reporter=line`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 115 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-svg-pdf-rendering.md` | Completed | Added visible SVG image rendering for selected-layer PDF artifacts. The web import path now falls back from `createImageBitmap` to browser `Image` decoding for SVG dimensions, server asset storage accepts and serves validated `image/svg+xml` bytes, and the browser artifact loader converts fetched SVG assets into PDF-only PNG previews while `pdfForNode` preserves the original SVG through `/EmbeddedFiles`. Broader raster/effect fidelity, richer annotations, webhooks/API stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN server asset storage, `pnpm --filter @layo/web test -- src/node-artifacts.test.ts`, focused Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "svg image artifacts" --reporter=line`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 116 passing tests, and `git diff --check`. |
| `2026-06-27-dev-panel-ready-annotations.md` | Completed | Added Penpot-inspired ready-for-dev annotation rows to structured code export and the visible Inspector Dev tab. Every exported structure node now carries identity, geometry, style, content, layout, component, and asset handoff annotations where applicable; the Dev Panel renders the selected node annotations and can copy them to the clipboard. Broader raster/effect export fidelity, webhook/API integration stories, and repo component mappings remain later developer-handoff gaps. Verified with RED/GREEN `pnpm --filter @layo/server test -- src/code-export.test.ts`, RED/GREEN Playwright CLI `pnpm exec playwright test apps/web/e2e/editor-mvp.spec.ts --grep "inspector dev panel shows selected layer handoff specs and code|inspector dev panel copies generated handoff snippets to the clipboard" --reporter=line`, and full-suite verification recorded in the implementation plan. |
| `2026-06-27-dev-component-mappings.md` | Completed | Added first-class saved repo component mappings across the server/Rust/renderer document contracts, HTTP `GET`/`PUT /files/:fileId/code-mappings`, MCP `list_code_component_mappings`/`set_code_component_mappings`, structured code export repoMapping artifacts for component definitions and instances, and a visible Inspector Dev Panel Code mapping card with import/usage copy. Variant-aware mappings, webhooks/API integration stories, plugin integration stories, and broader raster/effect fidelity remain later maturity gaps. Verified with RED/GREEN server export/HTTP/MCP tests, Rust JSON round-trip coverage, focused Playwright CLI mapping coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, web/server/full typechecks, web build, `cargo test --workspace`, `pnpm test`, `pnpm test:e2e`, and `git diff --check`. |
| `2026-06-27-variant-code-mappings.md` | Completed | Added variant-aware repo component mappings across server/Rust/renderer document contracts, HTTP persistence, MCP schema, generated TypeScript bindings, structured code export artifacts, and the visible Inspector Dev Panel Code mapping card. Saved `variant_props` now resolve component variant properties into usage such as `<Card title={title} surface="elevated" />`, while remaining design-system gaps are token sets/modes, typography tokens, reusable styles, full component variant UI, hosted library sync, plugin stories, and broader raster/effect fidelity. Verified with RED/GREEN server export/HTTP/MCP tests, Rust JSON round-trip and `ts-rs` binding export, focused Playwright CLI mapping coverage, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm test`, `pnpm test:e2e` with 117 passing tests, and `pnpm test:e2e:collab` with 5 passing tests. |
| `2026-06-27-component-variant-selection.md` | Completed | Added selected component-instance variants across renderer/server/Rust contracts, generated TS bindings, HTTP variant definition and instance selection routes, undo/redo-safe web editor state, right Inspector variant property selects, server persistence after Inspector changes, and selected-variant repo mapping usage in Dev Panel export. Remaining design-system gaps are token sets/modes, typography tokens, reusable styles, variant authoring/combination management, hosted library sync, plugin stories, and broader raster/effect fidelity. Verified with RED/GREEN web state, Rust JSON/binding, server export/HTTP/storage tests, `pnpm --filter @layo/web build`, focused Playwright CLI Inspector-to-Dev-panel proof, `pnpm test`, and `pnpm test:e2e` with 117 passing tests. |
| `2026-06-27-component-variant-authoring.md` | Completed | Added Penpot-inspired selected-main-component variant authoring to the right Inspector. Main components now expose editable variant name and first property/value rows, an add-variant command, undo/redo-safe `set_component_variants` editor state, queued HTTP persistence through the existing component variants route, and instance controls that consume authored values after reload. Remaining design-system gaps are token sets/modes, typography tokens, reusable styles, multi-property variant combination management, hosted library sync, plugin stories, and broader raster/effect fidelity. Verified with RED/GREEN web state, focused Playwright CLI authoring proof, `pnpm --filter @layo/web test -- src/editor-state.test.ts`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `pnpm test`, `pnpm test:e2e` with 118 passing tests, `pnpm typecheck`, `git diff --check`, and `pnpm test:e2e:collab` with 5 passing tests after starting the local relay. |
| `2026-06-27-component-variant-combinations.md` | Completed | Added Penpot-inspired multi-property component variant authoring to the selected main component Inspector. Property rows can be added across all variants, property names stay aligned across combinations, each variant keeps independent values, newly added variants inherit the current property schema, and selected instances consume the authored property combinations through existing variant selects. Remaining design-system gaps are token sets/modes, typography tokens, reusable styles, richer variant property types, matrix-level variant management, and hosted library registry/update sync. Verified with RED/GREEN focused Playwright CLI, `pnpm --filter @layo/web typecheck`, `pnpm test`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `pnpm test:e2e` with 119 passing tests, headed Playwright CLI proof for the multi-property variant authoring flow, and `pnpm test:e2e:collab` with 5 passing tests after starting the local relay. |
| `2026-06-27-variant-boolean-controls.md` | Completed | Added Penpot-inspired selected-instance boolean variant controls. Component instance properties with exactly two opposing values (`true/false`, `yes/no`, or `on/off`) render as toggles instead of dropdowns, while non-boolean properties keep existing selects. Remaining design-system gaps are token sets/modes, typography tokens, reusable styles, matrix-level variant management, and hosted library registry/update sync. Verified with RED/GREEN focused Playwright CLI, focused variant regression Playwright CLI, `pnpm --filter @layo/web typecheck`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `pnpm --filter @layo/web build`, `git diff --check`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e` with 120 passing tests, headed Playwright CLI proof for the boolean toggle flow, and `pnpm test:e2e:collab` with 5 passing tests after starting the local relay. |
| `2026-06-27-variant-matrix-overview.md` | Completed | Added Penpot-inspired selected-main-component variant matrix overview. The right Inspector shows variant rows against property columns and warns when two completed variants share the same property-value combination, while existing row editors keep the saved `ComponentVariant.properties[]` model. Remaining design-system gaps are token sets/modes, typography tokens, reusable styles, advanced matrix editing, and hosted library registry/update sync. Verified with RED/GREEN focused Playwright CLI, focused variant regression Playwright CLI, `pnpm typecheck`, `pnpm test`, `pnpm --filter @layo/web build`, `pnpm run check:penpot-maturity`, `pnpm run check:design-rules`, `git diff --check`, `pnpm test:e2e` with 121 passing tests, headed Playwright CLI proof for the matrix warning flow, and `pnpm test:e2e:collab` with 5 passing tests after starting the local relay. |
| `2026-06-28-penpot-token-themes-baseline.md` | Completed | Added a Penpot-inspired document-local token theme baseline. Token themes now round-trip through renderer/server/Rust document contracts, DTCG `$themes` and `$metadata.activeThemes`, server agent and MCP `set_token_theme_enabled`, canvas inspection/validation/code export metadata, undo/redo-safe web state, right Inspector grouped theme toggles, and bound fill/typography/layout spacing rematerialization. Rich theme editing, visual theme matrix management, hosted theme/library sync, and full library-level theme governance remain later design-system maturity gaps. Verified with focused RED/GREEN server, Rust, web reducer, Playwright CLI coverage, full repository gates recorded in the implementation plan, and squash merge PR #163 as `8d222cf4519e5f4d37a3906aa869e27d73a2b831`. |
| `2026-06-28-penpot-shadow-token-fidelity.md` | Completed | Added Penpot-inspired shadow tokens through Layo's existing token and single CSS box-shadow path. DTCG shadow composite import/export, agent `set_effect_shadow_token`, validation, right Inspector binding, saved persistence, Rust round-trip, active-token materialization, and CSS-variable code export now exist. Multi-effect stacks, reusable effect styles, richer effect export fidelity, and hosted theme/library registry sync remain later design-system maturity gaps. Verified with focused RED/GREEN server/web/Rust tests, focused Playwright CLI proof, `pnpm run check:design-rules`, `pnpm run check:penpot-maturity`, `pnpm typecheck`, `pnpm --filter @layo/web build`, `cargo test -p editor-core`, `pnpm test`, `pnpm test:e2e` with 137 passing tests, and `git diff --check`. |
| `2026-06-28-penpot-effect-styles.md` | Completed | Added Penpot-inspired reusable effect styles for Layo's single CSS box-shadow path. Document-local `effect` styles, `style.effect_shadow_style`, agent/MCP `set_effect_shadow_style`, validation, right Inspector create/apply controls, usage summaries, undo/manual override cleanup, Rust round-trip and generated bindings, persistence after reload, and CSS/structure code-export metadata now exist. Multi-effect stacks, richer effect export fidelity, and hosted theme/library registry sync remain later design-system maturity gaps. Verified with focused RED/GREEN server/web/Rust tests, generated binding export, focused Playwright CLI proof, and full repository gates recorded in the implementation plan. |
| `2026-06-28-penpot-team-scoped-registry.md` | Completed | Adapted Penpot's team-library permission expectation to Layo's local-first project sharing model. Published registry entries now carry source team ids, file-scoped registry lists hide other-team/private targets, cross-team review/import/update attempts are rejected, and storage/HTTP/MCP/web API helpers plus the visible file-panel UI are covered. Verified with focused RED/GREEN storage, HTTP, MCP, web API, and Playwright CLI tests; focused registry e2e; `pnpm run check:penpot-maturity`; `pnpm run check:design-rules`; `pnpm typecheck`; `pnpm --filter @layo/web build`; `cargo test -p editor-core`; `pnpm test`; `pnpm test:e2e` with 144 passing tests; and `git diff --check`. PR #188 was squash-merged as `8d5c608d540f9fd6eec25cc8bb10ddf73853c3ee`, local main was fast-forwarded, and the feature worktree plus local/remote feature branches were removed. |
| `2026-06-26-vercel-fullstack-deployment.md` | Completed | Vercel full-stack routing merged in PR #95, the incorrect GitHub About homepage was removed, and the current manual production deployment is verified at `https://layo-three.vercel.app/` under `jsleemasters-projects/layo` with same-origin `/health`. The remaining operations gap is CI-owned repeatability: GitHub repository Vercel secrets still need to be configured so `.github/workflows/vercel-production.yml` can deploy and smoke-test its own returned URL. |

## Handoff Plan

PR #15, `dc090c8`, added `AGENTS.md` and `docs/PROJECT_BRIEF.md`. Those files
summarize the implemented product state and should be treated as the current
agent handoff layer.

For Figma parity work, read `docs/product/figma-feature-inventory.md` and
`docs/product/figma-migration-roadmap.md` before creating or resuming a plan.
