# Superpowers Plan Status

Last audited: 2026-07-13

This file is the routing source of truth for `docs/superpowers/plans/*`.
The individual plan files are historical execution plans. Some older files still
contain unchecked task boxes even though their goals have since been implemented
and merged. Do not restart a historical plan only because its original checkboxes
were not backfilled.

A plan is active only when it is listed in `Current Active Plan`. All other plan
files are historical context unless a new branch or PR explicitly reopens the
same failed case through the Penpot maturity loop.

The pre-cleanup completion ledger is preserved in Git history at commit
`ca7e91fac6d934ca2c6bce6df563111630d1b542`. Use that commit when exact older
row evidence is needed. Keep this file focused on current routing plus recent
Penpot maturity evidence so stale rows do not misdirect future work.

Use this order when entering the repo:

1. Read `AGENTS.md` for current agent operating rules.
2. Read `docs/PROJECT_BRIEF.md` for the product summary.
3. Read this file before treating any individual plan as active work.
4. Open the specific plan only for implementation details or historical context.

## Current Active Plan

| Plan | Status | Evidence |
| --- | --- | --- |
| `2026-07-13-penpot-library-update-conflict-recovery.md` | Active | PR #283-#289 establish cross-file ownership, conflict preflight, immutable review/apply snapshots, caught-failure rollback, writer serialization, and target/subscription optimistic identity. PR #290 adds a durable original/intended write-ahead journal, atomic replacement for journaled asset/target/subscription paths, startup rollback, and external-byte conflict preservation. RED `29266760052` proved restart exposed partial target state; GREEN `29267673690` passed 251 web tests, 273 server tests, Rust workspace tests, and 192 Playwright cases. Cross-process mutation locking and stale-lock recovery remain active. Deployment remains non-gating. |

## Completed Plans

| Plan | Status | Evidence |
| --- | --- | --- |
| `2026-07-13-penpot-component-library-swap-migration.md` | Completed | PR #283 maps every packaged Penpot file into one component relation graph, persists source libraries as project documents and registry subscriptions, preserves nested `component_swap` overrides and cross-file image paints/assets, refreshes target definitions without losing swaps, and blocks missing sources before writes. RED runs `29244805795`, `29245285835`, `29245799016`, and `29246130322` proved review, structure, ownership, and persistence gaps; `29246894374` and `29247968681` drove reload re-entry and stable materialized-child identity fixes. Full Verification `29248081639` passed maturity/design gates, typecheck, build, 250 web tests, 262 server tests, Rust workspace tests, and 192 Playwright CLI cases. Product evidence is `docs/product/penpot-component-library-swap-migration-delta.md`. Conflict and rollback recovery is routed next; deployment remains non-gating. |
| `2026-07-13-penpot-component-instance-migration.md` | Completed | PR #282 preserves Penpot main definitions, linked instances, variant property combinations, and text/fill/stroke/opacity/geometry overrides with deterministic source ids. Dangling or ambiguous roots and connected layers now block before writes. RED runs `29240718967`, `29241301919`, `29241748384`, and `29242451060` proved each gap; failure-learning run `29242734727` corrected the validation response assumption. Full Verification `29243082186` passed maturity/design gates, typecheck, build, 250 web tests, 258 server tests, Rust workspace tests, and 191 Playwright CLI cases. Product evidence is `docs/product/penpot-component-instance-migration-delta.md`. External component libraries and nested swaps are routed next; deployment remains non-gating. |
| `2026-07-13-penpot-first-class-fill-paints.md` | Completed | PR #281 adds authoritative ordered solid/gradient/image fill ownership across Rust/TypeScript models, validated MCP/HTTP mutation and inspection, Yjs, component overrides, Korean Inspector lifecycle, canvas composition, SVG/PDF alpha and blend resources, PNG/JPEG/WEBP downloads, Penpot migration without synthetic image children, and structured code handoff. RED `29234879563` proved scalar artifacts; failure learning repaired migration expectations, Yjs setup, PDF mask ordering, raster coverage, and the missing fill-stack editor-history equality. Full Verification `29239015361` passed every gate with 250 web and 252 server tests; Playwright reported 189 passed plus one retried existing flaky case. Restore `29239015644` and retention `29239015484` passed. Product evidence is `docs/product/penpot-first-class-fill-paints-delta.md`. Penpot component/instance migration is routed next; deployment remains non-gating. |
| `2026-07-13-penpot-first-class-stroke-paints.md` | Completed | PR #280 adds authoritative per-stroke solid/gradient/image ownership across Rust/TypeScript models, validated MCP/HTTP mutation, Yjs, component/code handoff, Korean Inspector controls, canvas linear/radial/image rendering, SVG/PDF resources, raster proof, and Penpot migration without synthetic stroke-image children. RED `29228755403` proved solid fallback artifacts; failure learning repaired Rust insertion, raw color, helper collision, Yjs setup, legacy migration expectations, dash import, radial width/points, validation order, raster sampling, and active-input undo routing. Full Verification `29232502532` passed all gates and 189 Playwright cases in 6.8m; restore `29232502537` and retention `29232502595` passed. Product evidence is `docs/product/penpot-first-class-stroke-paints-delta.md`. Ordered fill paint ownership is routed next; deployment remains deferred. |
| `2026-07-13-penpot-closed-path-stroke-alignment.md` | Completed | PR #279 adds one shared closed-subpath resolver, geometry-aware Path2D canvas clipping, SVG clip/mask alignment, PDF even-odd clipping, PNG bounds, server-side open-path normalization, effective Inspector states, undo/redo, persistence, and reload. RED `29225628767` proved absent SVG/PDF/open-path behavior; failure learning fixed fill-rule routing, remote large-file boundary corruption, narrowing, E2E registration, and disabled-option matcher semantics. Full Verification `29227360608` passed all gates and 188 Playwright cases; restore `29227360595` and retention `29227360638` passed. Product evidence is `docs/product/penpot-closed-path-stroke-alignment-delta.md`. Per-stroke gradient/image paint ownership is routed next; deployment remains deferred. |
| `2026-07-13-penpot-first-class-multi-stroke.md` | Completed | PR #278 adds ordered per-stroke color, opacity, width, position, style, visibility, dash, cap/join, and endpoints across Rust/TypeScript models, validated MCP/HTTP style mutation, persistence, editor history, Yjs preservation, component overrides, Korean Inspector lifecycle, Konva/SVG/PDF paint passes, PNG bounds, and code handoff. Failure learning fixed legacy PDF coordinate shifts, raw UI color, scalar-only overrides, missing E2E registration, a mistaken lifecycle expectation, duplicate canvas painting, missing PNG/undo proof, and vector dash defaults. Full Verification `29224618439` passed all gates and 187 Playwright cases; restore `29224618444` and retention `29224618447` passed. Product evidence is `docs/product/penpot-first-class-multi-stroke-delta.md`. Closed curved path alignment is routed next; deployment remains deferred. |
| `2026-07-11-penpot-pdf-stroke-marker-fidelity.md` | Completed | PR #277 draws all supported endpoint markers as tangent-aware PDF geometry, expands first-class path SVG/PDF bounds for strokes and markers, exposes Korean stroke color/width/cap/join/dash/endpoints, preserves the full style contract through validated server normalization, serializes rapid style writes, and persists undo/redo. Failure runs fixed narrowing, path-bound expectations, dropped fields, persistence races, reducer no-op coverage, form-focus evidence, and tight PNG assumptions. Full Verification `29221738942` passed all gates and 186 Playwright cases. Product evidence is `docs/product/penpot-pdf-stroke-marker-fidelity-delta.md`. Deployment remains deferred. |
| `2026-07-11-penpot-open-path-flattening.md` | Completed | PR #276 preserves open line/cubic/compound geometry without invented closures and carries stroke width, cap, join, dash, endpoint-marker, and opacity contracts through Rust/TypeScript models, MCP/HTTP dry-run/apply, no-write mismatch guards, history, reload, canvas, SVG, PDF stroke operators, and PNG. Failure-learning runs corrected Paper open-area assumptions, cubic extrema, implicit SVG defaults, and two browser locator assumptions. Full Verification `29154359661` passed all gates and 186 Playwright cases; Storage Restore Drill `29153711191` passed. Product evidence is `docs/product/penpot-open-path-flattening-delta.md`. Deployment remains deferred. |
| `2026-07-10-penpot-path-flattening.md` | Completed | PR #275 adds Paper.js standalone closed-path flattening, deterministic MCP/HTTP `flatten_path` dry-run/apply, no-write invalid geometry handling, relationship/source removal after successful evaluation, Korean direct control, `Ctrl/Cmd+E`, undo/redo, persistence/reload, and SVG/PDF/PNG proof. RED run `29150104717` failed on the absent evaluator; failure-loop runs corrected rotation math and wrapped HTTP response assumptions; GREEN code-head Full Verification `29150553848` passed maturity/design gates, typecheck, web build, Core tests, and 185 Playwright tests. Final head verification is the merge gate. Product evidence is `docs/product/penpot-path-flattening-delta.md`. |
| `2026-07-10-penpot-nondestructive-boolean-paths.md` | Completed | PR #274 adds first-class non-destructive union, difference, intersection, and exclusion with ordered recoverable operands, Paper.js curved geometry, deterministic path/boolean MCP and HTTP commands, runtime validation, contextual Korean controls, undo/redo, canvas, SVG/PDF/PNG fidelity, and direct Playwright lifecycle proof. Full Verification #546 run `29100227669` passed maturity/design gates, typecheck, web build, Core tests, and the full Playwright suite. Product evidence is `docs/product/penpot-nondestructive-boolean-paths-delta.md`. |
| `2026-07-10-penpot-first-class-path-editing.md` | Completed | PR #272 landed the first-class Rust/TypeScript path contract, asset-free Penpot import, MCP/HTTP mutation, inspect/validation/change summaries, canvas/artifact fidelity, and direct anchor/control editing. PR #273 added line and Bezier anchor insertion, delete, corner/curve, join/merge/separate, explicit redo persistence, and Bezier browser dragging. Full Verification #478 run `29089414317` passed maturity/design gates, typecheck, web build, core tests, and full Playwright. Product evidence is `docs/product/penpot-first-class-path-editing-delta.md`. |
| `2026-07-06-penpot-radial-stroke-raster-canvas.md` | Completed | Adapts supported Penpot radial stroke paint sources to live canvas rendering and selected-layer PNG raster artifacts by resolving stroke radial paint sources through the shared radial gradient helper and drawing a Konva radial overlay with an inner fill mask. The regression is `apps/web/e2e/external-migration-penpot-stroke-gradient.spec.ts`; RED Full Verification #28783504718 failed only in Playwright CLI e2e because `centerStroke.r` was 128 instead of greater than `centerStroke.b + 40` (expected greater than 168). Implementation repair Full Verification #28784248696 failed at the design-rule gate on a raw fallback color and was fixed by using `editorKonvaTokens.selection.stroke`. GREEN Full Verification #28784374979 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m47s. Product delta is recorded in `docs/product/penpot-radial-stroke-raster-canvas-delta.md`. Remaining gaps are non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-width-raster-canvas.md` | Completed | Extends the supported Penpot radial raster/canvas path from circular gradients to width/ellipse geometry by rasterizing the Penpot width/angle transform into a Konva fill pattern for selected-layer canvas and PNG artifacts. The regression is `apps/web/e2e/external-migration-penpot-stroke-gradient.spec.ts`; RED Full Verification #28779327961 failed in Playwright CLI e2e before implementation because `verticalProbe.b` was 136 instead of greater than `verticalProbe.r + 40`. GREEN Full Verification #28780614235 passed after the implementation and type-compatibility repair; Storage Restore Drill #28780614255 and Storage Backup Retention #28780614275 also passed. PR #260 was squash-merged as `630fd73b62e04aaa10e63409c0fc6f9f6ccacc53`. Product delta is recorded in `docs/product/penpot-radial-width-raster-canvas-delta.md`. Remaining gaps are non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-raster-canvas.md` | Completed | Adapts preserved Penpot circular radial fill paint sources to live canvas rendering and selected-layer PNG raster artifacts for supported non-text, non-group rectangle nodes through Konva radial gradient props. The regression is `apps/web/e2e/external-migration-penpot-stroke-gradient.spec.ts`; RED Full Verification #28777522435 failed in Playwright CLI e2e before implementation because the downloaded PNG still sampled the flattened midpoint fallback color at the gradient center. Final PR-head Full Verification is the merge gate for this slice. Product delta is recorded in `docs/product/penpot-radial-raster-canvas-delta.md`. Remaining gaps are radial width/ellipse canvas parity, radial stroke-specific proof, non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-width-pdf-artifact.md` | Completed | Adapts preserved Penpot radial width geometry to emit selected-layer PDF `/ShadingType 3` radial shading resources with a PDF transform matrix so the supported elliptical radial shape survives export instead of falling back to a flattened solid fill. The regression is `apps/web/src/node-artifacts-radial-pdf.test.ts`; RED Full Verification #28776181108 failed in Core tests before implementation because the expected transformed radial shading resource was absent. Final PR-head Full Verification is the merge gate for this slice. Product delta is recorded in `docs/product/penpot-radial-width-pdf-artifact-delta.md`. Remaining gaps are radial stroke-specific proof, radial raster/canvas parity, non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-pdf-artifact.md` | Completed | Adapts preserved Penpot circular radial fill metadata to emit selected-layer PDF `/ShadingType 3` radial shading resources instead of falling back to a flattened solid fill. The regression is `apps/web/src/node-artifacts-radial-pdf.test.ts`; RED Full Verification #28775135264 failed in Core tests before implementation because the expected radial shading resource was absent. Final PR-head Full Verification is the merge gate for this slice. Product delta is recorded in `docs/product/penpot-radial-pdf-artifact-delta.md`. Remaining gaps are radial stroke-specific proof, radial width/ellipse PDF parity, radial raster/canvas parity, non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-width-svg-artifact.md` | Completed | Adapts preserved Penpot radial gradient width geometry to emit selected-layer SVG `gradientTransform` on radial paint servers, preserving the supported elliptical/rotated radial shape while retaining deterministic fallback fills. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28774007613 failed in Core tests before implementation because the expected `gradientTransform` was absent. Final PR-head Full Verification is the merge gate for this slice. Product delta is recorded in `docs/product/penpot-radial-width-svg-artifact-delta.md`. Remaining gaps are radial stroke-specific proof, radial PDF/raster/canvas parity, non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-06-penpot-radial-gradient-svg-artifact.md` | Completed | Adapts preserved Penpot radial fill paint-source metadata to emit selected-layer SVG `<radialGradient>` paint servers for supported simple circular non-text, non-group nodes while keeping deterministic `data-fallback-fill` and leaving PDF/raster/canvas radial parity as follow-up work. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28751033299 failed in Core tests before implementation, and GREEN PR-head Full Verification #28772353798 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m57s. Storage Restore Drill #28772353810 and Storage Backup Retention #28772353751 passed for the same head. Product delta is recorded in `docs/product/penpot-radial-gradient-svg-artifact-delta.md`. Remaining gaps are elliptical/rotated radial width geometry, radial stroke/PDF/raster/canvas parity, non-rect/path/group/text radial artifacts, exact mixed-stack overlay/blend compositing, image-gradient interactions, masks, paths, SVG raw shapes, components, variants, tokens, and shared library relations; deployment remains intentionally deferred. |
| `2026-07-05-penpot-gradient-raster-artifact.md` | Completed | Uses preserved Penpot simple linear fill/stroke gradient paint source metadata to render supported non-text, non-group rectangular nodes through Konva fill/stroke linear gradient props, so selected-layer PNG raster artifacts no longer fall back to midpoint purple strokes. The regression is `apps/web/e2e/external-migration-penpot-stroke-gradient.spec.ts`; RED Full Verification #28749312749 failed in Playwright CLI e2e before implementation, and GREEN Full Verification #28749797013 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m38s. Product delta is recorded in `docs/product/penpot-gradient-raster-artifact-delta.md`. Remaining gaps are radial/non-linear gradients, exact mixed-stack overlay/blend compositing, image-gradient interactions, non-rect/path gradient rendering, group/text gradient rendering, and deeper multi-paint stack parity; deployment remains intentionally deferred. |
| `2026-07-05-penpot-stroke-gradient-pdf-artifact.md` | Completed | Uses preserved Penpot stroke linear gradient paint source metadata to emit selected-layer PDF axial shading resources and clipped stroke-ring `/Sh1 sh` commands while suppressing fallback solid stroke commands for the supported slice. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28748110842 failed in Core tests before implementation, and GREEN Full Verification #28748276845 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m34s. Product delta is recorded in `docs/product/penpot-stroke-gradient-pdf-artifact-delta.md`. Raster artifacts, live canvas rendering, radial geometry, stop-specific alpha, blend modes, exact multi-paint compositing, and deeper mixed gradient/stroke paint stacks remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-stroke-gradient-svg-artifact.md` | Completed | Uses preserved Penpot stroke linear gradient paint source metadata to emit selected-layer SVG artifact `<linearGradient>` paint servers and `stroke=url(#...)` while retaining deterministic `data-fallback-stroke`; ids include paint kind to prevent fill/stroke index collisions. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28747081513 failed in Core tests before implementation, and GREEN Full Verification #28747291400 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m52s. Product delta is recorded in `docs/product/penpot-stroke-gradient-svg-artifact-delta.md`. PDF/raster stroke-gradient artifacts, live canvas rendering, radial geometry, stop-specific alpha, blend modes, and exact multi-paint compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-gradient-pdf-artifact.md` | Completed | Uses preserved Penpot fill linear gradient paint source metadata to emit selected-layer PDF axial shading resources and clipped `/Sh1 sh` fills while retaining deterministic fallback behavior for unsupported gradients. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28746017528 failed in Core tests before implementation, and GREEN Full Verification #28746327138 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m38s. Product delta is recorded in `docs/product/penpot-gradient-pdf-artifact-delta.md`. Raster artifacts, live canvas rendering, radial geometry, stroke-gradient artifacts, stop-specific PDF alpha, blend modes, and exact multi-paint compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-gradient-svg-artifact.md` | Completed | Uses preserved Penpot fill linear gradient paint source metadata to emit selected-layer SVG artifact `<linearGradient>` paint servers and `fill="url(#...)"` while retaining deterministic `data-fallback-fill`. The regression is `apps/web/src/node-artifacts-gradient.test.ts`; RED Full Verification #28744752449 failed in Core tests before implementation, first GREEN attempt #28744966412 failed at typecheck on optional gradient narrowing, and final GREEN Full Verification #28745127711 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 6m28s. Product delta is recorded in `docs/product/penpot-gradient-svg-artifact-delta.md`. PDF/raster artifacts, live canvas rendering, radial geometry, stroke-gradient artifacts, blend modes, and exact multi-paint compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-gradient-source-css-export.md` | Completed | Uses preserved Penpot fill gradient paint source metadata to add CSS `background-image: linear-gradient(...)` in global and per-element code export CSS while keeping flattened `background-color` fallback and existing structure `paintSources`. The regression covers global CSS, per-element CSS, structure metadata, annotation presence, and generated JS module serialization in `apps/server/src/code-export.penpot-paint-source.test.ts`. Product delta is recorded in `docs/product/penpot-gradient-source-css-export-delta.md`. Full Verification #28743933385 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m48s on code head `0054d9bc4b8df9fc5772459e4a811f8e89f778e8`. Local shell verification remains unavailable in this Codex session because some commands exit 134. Live canvas gradient geometry, selected-layer artifact gradient fidelity, blend-mode compositing, radial geometry, stroke-gradient handoff, and exact multi-paint rendering/export remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-paint-source-metadata.md` | Completed | Preserves original Penpot fill/stroke paint source metadata under `style.paint_sources`, exposes it to agent inspection, and carries it into code-export structure annotations and generated JS modules while keeping deterministic flattened render styles. The regression covers direct import, HTTP persistence, agent inspection, code export structure, and JS module serialization in `apps/server/src/external-migration.penpot-paint-source.test.ts`. Product delta is recorded in `docs/product/penpot-paint-source-metadata-delta.md`. Full Verification #28743060413 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 8m3s on code head `bd83bf69e006e15629c456110193b92898575960`. Local shell verification remains unavailable in this Codex session because some commands exit 134. Exact visible gradient geometry, blend-mode compositing, and multi-paint rendering/export remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-alpha-artifacts.md` | Completed | Applies preserved Penpot mask source opacity in selected-layer SVG artifacts through SVG mask alpha and in selected-layer PDF artifacts through clipping-scope ExtGState alpha while keeping opaque polygon clipPath/PDF clipping-path behavior. The regression covers opaque vs translucent polygon behavior in `apps/web/src/node-artifacts-clip.test.ts`. Full Verification #28741884857 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m38s. Local shell verification remains unavailable in this Codex session because commands exit 134. Visible canvas rendering, selected-layer raster artifact masking, and exact blend/compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-polygon-pdf-artifact.md` | Completed | Uses preserved Penpot mask source points to emit selected-layer PDF artifact clipping paths while keeping the existing rectangular clipping fallback for bounds-only clips. The regression covers both fallback and polygon-source PDF behavior in `apps/web/src/node-artifacts-clip.test.ts`. Full Verification #28740500230 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 6m8s. Local shell verification remains unavailable in this Codex session because commands exit 134. Canvas rendering, selected-layer raster artifact masking, and alpha compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-polygon-svg-artifact.md` | Completed | Uses preserved Penpot mask source points to emit selected-layer SVG artifact `<polygon>` clipPath geometry while keeping the existing rect clipPath fallback for bounds-only clips. The regression covers both fallback and polygon-source behavior in `apps/web/src/node-artifacts-clip.test.ts`. Full Verification #28739510986 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m49s. Local shell verification remains unavailable in this Codex session because commands exit 134. Canvas rendering, PDF/raster artifact masking, and alpha compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-polygon-code-export.md` | Completed | Uses preserved Penpot mask source points to emit deterministic CSS `clip-path: polygon(...)` in global code export CSS, per-element CSS, and generated JS modules while keeping `overflow: hidden` and stable `clip: { type: "bounds" }`. The regression covers polygon CSS, annotation detail, element CSS, and serialized JS module output. Full Verification #28733326443 first attempt passed Penpot maturity/design gates, typecheck, web build, and Core tests, including the polygon regression, then failed Playwright CLI e2e on an unrelated `editor-mvp.spec.ts` component-variant flake; rerun job #85217867262 passed gates, typecheck, web build, Core tests, and Playwright CLI e2e in 5m15s. Local shell verification remains unavailable in this Codex session because commands exit 134. Canvas/artifact arbitrary mask rendering and alpha compositing remain next gaps; deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-source-metadata.md` | Completed | Preserves Penpot masked-group source metadata under the existing bounds clip primitive, including source shape id, name, type, bounds, opacity, and polygon points when present. The regression covers persistence, agent inspection, code export structure/annotation, and generated JS module output so handoff artifacts do not keep stale anonymous bounds clips. Full Verification #28732770022 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e in 7m59s on code head `bf291f46ceede1a83598689b7ffe4b1e12f4c222`. Local shell verification remains unavailable in this Codex session because commands exit 134. Arbitrary/alpha mask rendering remains a next gap and deployment remains intentionally deferred. |
| `2026-07-05-clip-agent-inspection-summary.md` | Completed | Exposes bounds clipping in deterministic agent node summaries by wrapping the existing agent-control implementation and overriding `inspectCanvas`, `findNodes`, and batch inspection output. The regression covers a Penpot clipped masked-group import and asserts persistence, agent inspection, and code handoff all carry `clip: { type: "bounds" }`. Full Verification #28731905701 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e; the e2e suite included the clipped masked-group browser scenario at `[159/177]` and finished with `177 passed (5.9m)`. Local shell verification remains unavailable in this Codex session because commands exit 134. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-mask-clipping-import.md` | Completed | Adapts Penpot masked-group imports from unclipped group containers to bounds-clipped group metadata plus code/SVG handoff. RED Full Verification #28730602500 failed before `RendererNode` supported `clip`; repair Full Verification #28730992728 passed gates/typecheck/build but failed Core tests on the old warning expectation and over-broad agent inspection assertion. GREEN code-head Full Verification #28731065239 passed gates, typecheck, web build, Core tests, and Playwright CLI e2e. Documentation-head Full Verification #28731268949 also passed with the clipping import scenario at `[159/177]` and `177 passed (6.0m)`. Arbitrary/alpha mask fidelity remains a next gap. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-masked-group-import.md` | Completed | Adapts Penpot masked groups by preserving them as Layo group containers with child layers instead of skipping the visible structure. RED Full Verification #28729630728 failed in Core tests because masked group import returned `mappedNodeCount: 0`, `skippedNodeCount: 1`, and warned `Skipped unsupported Penpot shape type group (Masked artwork).`; GREEN Full Verification #28729944594 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[159/177]` and `177 passed (5.7m)`. Storage Restore Drill #28729944610 and Storage Backup Retention #28729944607 passed for the same head. Mask clipping remains a deliberate import divergence and deployment remains intentionally deferred. |
| `2026-07-05-penpot-path-image-import.md` | Completed | Adapts Penpot `path` shapes by preserving simple readable SVG path data as local `image/svg+xml` assets and mapping the shape to a deterministic Layo image node. RED Full Verification #28728895566 failed in Core tests because the new cases imported only the frame (`mappedNodeCount: 1`, `skippedNodeCount: 1`) and HTTP `assetCount: 0`; GREEN Full Verification #28729068240 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[159/176]` and `176 passed (5.6m)`. Storage Restore Drill #28729068246 and Storage Backup Retention #28729068239 passed for the same head. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-svg-raw-image-import.md` | Completed | Adapts Penpot `svg-raw` shapes by preserving readable raw SVG content as local `image/svg+xml` assets and mapping the shape to a deterministic Layo image node. RED Full Verification #28728076290 failed in Core tests because the new cases still imported only the frame (`mappedNodeCount: 1`, `skippedNodeCount: 1`) and HTTP `assetCount: 0`; GREEN Full Verification #28728279896 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[162/175]` and `175 passed (5.7m)`. Storage Restore Drill #28728279895 and Storage Backup Retention #28728279907 passed for the same head. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-frame-dual-image-paints.md` | Completed | Adapts a Penpot frame that has both packaged `fill-image` and packaged `stroke-image` records by preserving both referenced assets as deterministic frame image children before foreground layers. RED Full Verification #28727329333 failed in Core tests because the new dual image-paint cases received `mappedNodeCount: 3` and HTTP `assetCount: 1`; GREEN Full Verification #28727453802 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[158/174]` and `174 passed (5.8m)`. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-frame-stroke-image-assets.md` | Completed | Adapts packaged Penpot frame `stroke-image` records by preserving the referenced image as a deterministic frame image child in local asset storage while keeping foreground children intact. RED Full Verification #28726006772 failed on `mappedNodeCount: 2` instead of 3 and HTTP `assetCount: 0`; GREEN Full Verification #28726205968 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[157/173]` and `173 passed (5.5m)`. Storage Restore Drill #28726205973 and Storage Backup Retention #28726205979 passed for the same head. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-stroke-image-assets.md` | Completed | Adapts packaged Penpot rect `stroke-image` records by preserving the referenced image as a Layo image node in local asset storage. RED Full Verification #28725171703 failed on importedAssets length 0 and HTTP assetCount 0; GREEN Full Verification #28725424332 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e with the new spec included at `[158/172]`. Storage Restore Drill #28725424329 and Storage Backup Retention #28725424333 passed for the same head. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-mixed-gradient-stroke-stack.md` | Completed | Adds server, HTTP persistence, and Playwright CLI coverage for a Penpot stroke stack mixing a front solid red stroke with a back `stroke-color-gradient` record. Full Verification #28724576060 passed, confirming the existing importer flattens the stack to `style.stroke: #804040` with `stroke_width: 6`; no importer code change was needed. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-different-width-stroke-stack.md` | Completed | Adapts Penpot solid `strokes` stacks with different `stroke-width` values by preserving the widest valid stroke width as Layo's single `style.stroke_width` while keeping the existing flattened stroke color path. RED Full Verification #28723902377 failed on `stroke_width: 2` instead of 8; GREEN Full Verification #28724082389 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-solid-multi-stroke-flattening.md` | Completed | Adapts Penpot same-width solid `strokes` stacks by flattening front-to-back paint order into one deterministic Layo `style.stroke` color and opacity. RED Full Verification #28722912062 failed on `#ff0000` instead of `#800080`; GREEN Full Verification #28722958842 passed after implementation; final docs/head Full Verification #28723174583, Storage Restore Drill #28723174585, and Storage Backup Retention #28723174586 passed. PR #231 was squash-merged as `ca7e91fac6d934ca2c6bce6df563111630d1b542`. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-stroke-gradient-flattening.md` | Completed | Adapts Penpot `stroke-color-gradient` stop vectors by flattening midpoint color into deterministic Layo `style.stroke` while preserving stroke width. Initial RED Full Verification #28722244025 caught missing e2e script registration; RED Full Verification #28722292528 failed with `stroke: null` and `stroke_width: 0`; GREEN Full Verification #28722346733, Storage Restore Drill #28722346756, and Storage Backup Retention #28722346750 passed. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-mixed-fill-image-stack.md` | Completed | Adapts Penpot mixed color/gradient plus packaged `fill-image` stacks by scanning every `fills` record for importable image metadata before color/gradient fallback. RED Full Verification #28721329168 failed on importedAssets length 0 and HTTP assetCount 0; final Full Verification #28721565316, Storage Restore Drill #28721565313, and Storage Backup Retention #28721565300 passed. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-gradient-fill-flattening.md` | Completed | Adapts Penpot `fill-color-gradient` stop vectors by flattening midpoint color into deterministic Layo `style.fill` while preserving opacity. RED Full Verification #28719962150 failed because `Gradient card` imported as default `#e5e7eb` instead of `#800080`; GREEN Full Verification #28720033921, documentation cleanup #28720378844, and final Full Verification #28720605595 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-solid-multi-fill-flattening.md` | Completed | Adapts Penpot solid-color `fills` stacks by flattening front-to-back paint order into one deterministic Layo `style.fill` and opacity. RED Full Verification #28719300875 failed because Layo imported only `#ff0000` at opacity 0.5 instead of `#800080` at opacity 1; GREEN Full Verification #28719395620 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e. Deployment remains intentionally deferred. |
| `2026-07-05-e2e-script-coverage-guard.md` | Completed | Failure-learning loop for missed root e2e spec wiring after PR #224. Adds a root `pnpm test` guard that fails when non-collaboration `apps/web/e2e/*.spec.ts` files are absent from `test:e2e`, wires external migration and layout-item specs into Playwright CLI e2e, and fixes the external migration asset spec structure. RED Full Verification #28718600049 failed at the new plan-status structure assertion; GREEN Full Verification #28718647800 passed with the newly wired e2e files. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-frame-fill-image-background.md` | Completed | Adapts Penpot packaged frame `fill-image` backgrounds by preserving frame hierarchy and adding a deterministic background image child before foreground layers. RED Full Verification #28717852745 failed on mappedNodeCount 2 and HTTP assetCount 0; GREEN Full Verification #28717969048 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e. Repair run #28717920390 caught escaped quote syntax from the remote update path and was fixed before final GREEN. Deployment remains intentionally deferred. |
| `2026-07-05-penpot-fill-image-paints.md` | Completed | Adopts Penpot packaged rectangle `fill-image` paints as Layo image nodes backed by local asset storage while preserving the existing image-shape path. RED Full Verification #28717101423 failed on importedAssets length 0 and HTTP assetCount 0; GREEN Full Verification #28717206611 passed Penpot maturity/design gates, typecheck, web build, Core tests, and Playwright CLI e2e. Deployment remains intentionally deferred. |
| `2026-07-05-plan-status-structure-guard.md` | Completed | Restores `PLAN_STATUS.md` as a single canonical Markdown document and adds `check:penpot-maturity` coverage that fails if the routing source of truth is prefixed by duplicate table fragments, has duplicate completed-plan headings, or concatenates table rows with the title. RED Full Verification #28716410739 failed at the new plan-status structure assertion; GREEN Full Verification #28716443097 passed after the document repair and guard. Deployment remains intentionally deferred. |

## Handoff Plan

PR #15, `dc090c8`, added `AGENTS.md` and `docs/PROJECT_BRIEF.md`. Those files
summarize the implemented product state and should be treated as the current
agent handoff layer.

For Figma parity work, read `docs/product/figma-feature-inventory.md` and
`docs/product/figma-migration-roadmap.md` before creating or resuming a plan.

For team-product maturity work, read `docs/product/penpot-maturity-benchmark.md`
and `docs/process/penpot-maturity-loop.md` before creating the next goal.
