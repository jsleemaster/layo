name: Codex Radial Width PDF Patch

on:
  push:
    branches:
      - codex/penpot-radial-width-pdf-artifact
    paths:
      - .github/workflows/codex-radial-width-pdf-patch.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/penpot-radial-width-pdf-artifact
      - name: Apply radial width PDF patch
        run: python3 scripts/codex-radial-width-pdf-patch.py
      - name: Commit patch and remove runner
        run: |
          rm scripts/codex-radial-width-pdf-patch.py .github/workflows/codex-radial-width-pdf-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/src/node-artifacts.ts scripts/codex-radial-width-pdf-patch.py .github/workflows/codex-radial-width-pdf-patch.yml
          git commit -m "feat: preserve radial width geometry in PDF artifacts"
          git push origin HEAD:codex/penpot-radial-width-pdf-artifact
