name: Codex Radial PDF Patch

on:
  push:
    branches:
      - codex/penpot-radial-pdf-artifact
    paths:
      - .github/workflows/codex-radial-pdf-patch.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/penpot-radial-pdf-artifact
      - name: Apply radial PDF patch
        run: python3 scripts/codex-radial-pdf-patch.py
      - name: Commit patch and remove runner
        run: |
          rm scripts/codex-radial-pdf-patch.py .github/workflows/codex-radial-pdf-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/src/node-artifacts.ts scripts/codex-radial-pdf-patch.py .github/workflows/codex-radial-pdf-patch.yml
          git commit -m "feat: emit radial PDF shading resources"
          git push origin HEAD:codex/penpot-radial-pdf-artifact
