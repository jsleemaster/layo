name: Codex Radial Width Docs Patch

on:
  push:
    branches:
      - codex/penpot-radial-width-svg-artifact
    paths:
      - .github/workflows/codex-radial-width-docs-patch.yml

permissions:
  contents: write

jobs:
  patch-docs:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/penpot-radial-width-svg-artifact
      - name: Apply docs patch
        run: python3 scripts/codex-radial-width-docs-patch.py
      - name: Commit docs patch and remove runner
        run: |
          rm scripts/codex-radial-width-docs-patch.py .github/workflows/codex-radial-width-docs-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/superpowers/PLAN_STATUS.md docs/product/penpot-maturity-benchmark.md scripts/codex-radial-width-docs-patch.py .github/workflows/codex-radial-width-docs-patch.yml
          git commit -m "docs: record Penpot radial width SVG maturity slice"
          git push origin HEAD:codex/penpot-radial-width-svg-artifact
